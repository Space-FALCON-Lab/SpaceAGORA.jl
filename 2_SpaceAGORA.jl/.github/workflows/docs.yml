name: Documentation

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: read
  statuses: write

jobs:
  docs:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      JULIA_NUM_THREADS: "2"
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          token: ${{ secrets.SPACEFALCON_REPO_READ_TOKEN || github.token }}

      # Prefer the dev tree: the public GRAMSuite's LFS bandwidth is easily
      # exhausted by CI (persistent 502s on the batch endpoint), while the
      # dev repo stores its data without LFS. Fall back to the public tree
      # when the read token cannot reach the dev repo.
      - name: Update GRAMSuite submodule to latest main
        env:
          GH_TOKEN: ${{ secrets.SPACEFALCON_REPO_READ_TOKEN || github.token }}
        run: |
          bash .github/scripts/update_gramsuite_submodule.sh dev \
            || bash .github/scripts/update_gramsuite_submodule.sh regular

      - name: Setup Julia
        uses: julia-actions/setup-julia@v2
        with:
          version: "1.12"

      - name: Cache Julia artifacts
        uses: julia-actions/cache@v2

      - name: Instantiate environment
        run: julia --startup-file=no --project=. -e 'using Pkg; Pkg.instantiate()'

      - name: Build and deploy docs
        run: julia --project=. docs/make.jl

      - name: Upload undocumented public API report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: undocumented-public-api-${{ github.run_id }}
          path: docs/build/undocumented_public_exports.txt
          if-no-files-found: warn
