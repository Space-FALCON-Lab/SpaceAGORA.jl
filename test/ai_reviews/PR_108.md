# AI Review Artifact for PR #108

## Scope
Dependency swap: the `SatelliteToolbox` meta-package is replaced by the three sub-packages the code uses (`SatelliteToolboxAtmosphericModels`, `SpaceIndices`, `SatelliteToolboxTransformations`). Same functions, same versions as before (they were already resolved in the manifest through the meta-package), so no numerical change.

## Changed Files
- src/environment/atmosphere/density_models.jl — `using SatelliteToolboxAtmosphericModels: AtmosphericModels` and `using SpaceIndices`; the three `SpaceIndices` call sites and the `nrlmsise00` call lose the `SatelliteToolbox.AtmosphericModels.` prefix.
- src/dynamics/coupled/perturbations.jl — `using SatelliteToolboxTransformations: ned_to_ecef` replaces `using SatelliteToolbox`.
- src/core/interfaces/reference_system.jl — `using SatelliteToolbox` removed; its only references were in comments.
- Project.toml / Manifest.toml — meta-package out, three sub-packages in; four orphaned manifest blocks removed; `project_hash` refreshed.
- test/gates/ci_stale_deps_gate.jl — `SatelliteToolboxGravityModels` added to the test-only ignore list with the reason.
- test/integration/runtests.jl, test/suites/01_contract_and_api_tests.jl — the NRLMSISE-00 reference calls go through `SatelliteToolboxAtmosphericModels` directly.
- docs/src/user/atmosphere_models.md, docs/src/user/troubleshooting.md — name the new packages.

## Findings
- P2: an unqualified toolbox export could have been used somewhere without a visible `SatelliteToolbox.` prefix. Checked: every exported name of `SatelliteToolbox` was scanned against `src/`, `test/suites`, `test/probes` and `test/smoke`; the only hits outside comments and docstrings are `ned_to_ecef` in perturbations.jl and the Dates re-exports, which the files already import from `Dates`.
- P2: `SpaceIndices.init(SpaceIndices.Celestrak; ...)` must be the same API the meta-package path reached. Checked: `SatelliteToolbox.AtmosphericModels.SpaceIndices` was this same module (`SpaceIndices` 2.1.0); the resolved version does not change.
- P3: hand-editing the manifest risks an inconsistent environment. Checked: `Pkg.instantiate()` reports no out-of-sync warning after the `project_hash` refresh, and the package loads without `SatelliteToolbox` in the loaded modules.

## P1 Assessment
No P1 findings. Unresolved P1: No

## Tests Added/Updated
- None added: suite 01 already compares the fixed-index and provider NRLMSISE-00 paths against the toolbox function and suite 04 keeps its harmonics reference. Manual: that suite-01 comparison replicated locally with zero relative error; `ned_to_ecef` confirmed to resolve to `SatelliteToolboxTransformations`; stale-deps gate and the full contract gate set pass.

## Residual Risk
Low. Downstream code that relied on `using SpaceAGORA` transitively loading `SatelliteToolbox` would need to add the dependency itself; nothing inside the repository does.
