Home for function-level unit tests migrated out of `test/suites/04_solver_env_and_regression_tests.jl` (rigid-body/rotational dynamics, gravity backbone, RHS split behavior) and `test/suites/03_persistence_units_and_rotational_tests.jl` (rotational-specific cases), mirroring `src/dynamics/`.

See `test/TEST_RESTRUCTURE_PLAN.md` for the migration order and `test/README.md` for the convention new tests here should follow.
