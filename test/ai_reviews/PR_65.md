# AI Review — PR 65: warn on link-vs-spacecraft wind DISAGREEMENT (PR #63 Codex follow-up)

## Scope
Codex P2 on the merged #63: the relocated warning fired on `any(!iszero, wind_link)`, which (a) stays silent for the case it exists to disclose when a calm link (zero local wind) sits inside a nonzero spacecraft-level wind — the link then wrongly inherits the spacecraft wind in `vel_pp_rw` — and (b) false-alarms when link and spacecraft winds are equal and nonzero, where using the spacecraft-level wind for the link is exactly correct.

## Changed Files
- `src/dynamics/coupled/aerodynamic_wrench_models.jl` — the predicate becomes elementwise disagreement between the valid link wind sample and the spacecraft-level `wind` (`!all(wind_link .== wind)`); the warning text and comment now state the disagreement semantics. Wind-disabled environments produce identical zero samples on both sides, so they can never warn — the "respect whether wind is enabled" concern is satisfied structurally. Diagnostics only; all force/torque computations byte-identical.

## Findings
- P2 (addressed): warn condition changed from "link wind nonzero" to "link wind ≠ spacecraft wind", which is precisely the set of evaluations where discarding the link sample changes the physics.
- No P1 findings.

## P1 Assessment
No P1 findings. Pure diagnostics-predicate change on an opt-in path; the strict floating-point comparison is deliberate — agreement means the same model evaluated to the same numbers (constant field or both zero), disagreement of any magnitude is a real spatial-wind difference.
Unresolved P1: No

## Tests Added/Updated
- None added: exercising the warning end-to-end still needs an orientation-enabled multi-link wrench with a spatially varying wind-bearing atmosphere (outside the probe file's scope, as recorded in PR_63.md); the "Per-link atmosphere scoping" testset continues to pin the gating semantics, and the full probe file is green locally.

## Residual Risk
- The complete per-link wind treatment (per-link wind-relative velocity through Mach/q/drag/lift/crosswind) remains the tracked future work from PR #62.
