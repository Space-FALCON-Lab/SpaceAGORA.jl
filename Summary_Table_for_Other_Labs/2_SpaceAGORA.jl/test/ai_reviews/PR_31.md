# AI Review — PR #31

## Scope
- Reviewed the env-config hoisting work: per-RHS-call environment parsing (shown by profiling to dominate the flat profile and serialize all in-process thread parallelism through the process-global ENV lock) is resolved once at `run_simulation` setup into three typed snapshots (`PolicyDecisionEnvConfig`, `RhsPlanEnvConfig`, `CallbackEnvConfig`) installed on SharedBuffers, with live-ENV fallback for hand-built `ODEParams` so unit tests keep read-at-use semantics.
- Reviewed the CI repairs riding with it: six auto-mode budget-floor pins in `test/coverage_threaded_probes.jl` (the Coverage Threaded Probe Driver re-runs that file at `--threads=2` under coverage, which is what CI.yml/nightly/coverage gates enable — the floors from e0015233 exceed CI thread counts), the July 2026 coverage-debt register in `test/ci_coverage_quality_gate.jl`, stale-path fixes in `docs/StateNormalization.md`, and the `data/GRAMSuite.jl` gitlink bump from the dangling `50809c58` to the reachable `8e075d48`.
- Review method: all 11 contract gates run locally (the architecture gate flips from red to green on this branch — the `solver_policy.jl` ENV read is removed); full integration suite (4081 assertions, 98 testsets, exit 0); the six pinned probe decisions validated true at `--threads=2` and confirmed blocked without pins, with expect-false neighbors unaffected; performance A/B on this exact branch.

## Changed Files
- `src/core/types/runtime_types.jl` — adds the three typed env-snapshot config structs.
- `src/parallel/policy/adaptive_decision.jl` — `thread_policy_decision` gains an `env=` kwarg; reads policy config from the snapshot.
- `src/parallel/policy/env_config.jl` — adds `snapshot_policy_decision_env`; parse helpers retained for fallback.
- `src/parallel/policy/parallel_policy.jl` — exports the snapshot API.
- `src/simulation/callbacks/control_callbacks.jl` — callback env reads via snapshot accessor.
- `src/simulation/callbacks/density_callbacks/config.jl` — density policy env reads via snapshot accessor.
- `src/simulation/callbacks/density_callbacks/model_selection.jl` — isolated-pool config via snapshot accessor.
- `src/simulation/callbacks/density_callbacks/runtime.jl` — runtime density path via snapshot accessor.
- `src/simulation/callbacks/gram_track_cache/config.jl` — track-cache config snapshotted (was parsed per call).
- `src/simulation/callbacks/gram_track_cache/interpolation.jl` — reads snapshotted track-cache config.
- `src/simulation/callbacks/registry.jl` — installs `_snapshot_callback_env_config` at setup.
- `src/simulation/callbacks/thermal_callbacks.jl` — thermal policy env reads via snapshot accessor.
- `src/simulation/engine/adapters/from_env.jl` — adds `_engine_env_get_with_env_fallback`/`_engine_env_haskey_with_env_fallback` so solver SAVE_* knobs keep ENV fallback inside override scopes.
- `src/simulation/engine/dynamics_rhs.jl` — RHS plan env reads via `_rhs_env_config(p)`.
- `src/simulation/engine/effector_sampling.jl` — effector policy env reads via snapshot.
- `src/simulation/engine/execution.jl` — calls `_initialize_runtime_env_config!(p)` during setup.
- `src/simulation/engine/setup.jl` — builds `RhsPlanEnvConfig`; `_rhs_execution_plan` reads the snapshot.
- `src/simulation/engine/solver_policy.jl` — removes direct `get(ENV, ...)` (the architecture-gate violation); uses the adapter fallback helpers.

## Findings
- P2 (fixed in-branch): the six coverage probes asserted auto-mode threading without pinning the budget floors raised by e0015233, making every coverage-enabled CI run red while local non-coverage runs stayed green. Fixed by per-block floor pins mirroring the suite-02 precedent from 28ebda02.
- P2 (fixed in-branch): `docs/StateNormalization.md` referenced retired `src/utils/`/`src/simulation_model/` paths, failing the canonical-path contract gate that fronts the CI contract suite.
- P3 (recorded, tracked): main overall coverage measured well below the original threshold; restore-to-90 is tracked follow-up work.
- No findings against the snapshot design itself: capture-at-run-start semantics are documented in the parallel-execution guide, and the fallback path preserves `withenv`-based unit-test behavior.

## P1 Assessment
- The architecture-gate violation (ENV read outside engine adapters) and the months-long CI redness were the candidate P1s; both are resolved by this PR.
- Unresolved P1: No

## Tests Added/Updated
- `test/coverage_threaded_probes.jl`: six auto-mode probe blocks gain budget-floor pins so they test mode/threshold/outer gating rather than the floor default; validated at `--threads=2` (all six true) and cross-checked that expect-false neighbors are unaffected.
- `test/ci_coverage_quality_gate.jl`: coverage debt register added (thresholds now bind again instead of the gate erroring before evaluation).
- Existing suites re-run as verification: full integration suite 4081/4081; all 11 contract gates green including `ci_architecture_contract_gate` (previously red) and `ci_canonical_path_contract_gate`.

## Residual Risk
- Env values are captured at run start: code that changed `ENV` mid-run and expected an in-flight simulation to react will no longer see the change. No such usage exists in the repo; the behavior is documented.
- The coverage debt register lowers per-file floors to measured reality; the risk of silent further drift is bounded by the floors themselves and by the tracked follow-up to raise them back with real tests.
- Measured GC share (~53% of threaded-ensemble wall time) is the next performance bottleneck after this change; allocation reduction in the solve path is follow-up work.
