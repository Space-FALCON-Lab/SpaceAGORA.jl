# PR 141: regional terrain queries

## Scope

Extract the terrain grid, query and local-file layer from Evan's PR121 (`cc891c76`) onto main `f0ccc21`. Regional planetocentric latitude/east-longitude queries return metres above an explicit reference sphere. Landing, altimetry, plume physics, viewer terrain, native atmosphere and mission assets are separate changes. Evan's attribution is retained.

## Changed Files

- `src/environment/terrain/terrain_models.jl`: canonical owner for grids/models, bilinear queries, longitude coverage, explicit datum consistency and strict local loaders.
- `src/core/types/abstract_types.jl`: the documented terrain extension interface.
- `src/core/simulation_model.jl`: include/reexport the terrain owner without adding aggregator behavior.
- `src/SpaceAGORA.jl`: root imports, exports and documentation forwarding to the owners.

## Findings

Independent bounded source review found no remaining blocker. Corrections made before final acceptance include: reject nonfinite and unrepresentable grids; reduce huge longitudes without loops; test canonical longitude intervals before shifting so just-outside seam queries are not rounded onto an edge; require resolvable cell centres; retain a model's datum on owned grids; and reject mismatched header, site-entry or explicit query radii. The raw local data are explicitly decoded as little-endian Float32 with validated dimensions and exact length. No default Moon radius is inferred.

The implementation and test/guide preparation were split between two agents. A separate reviewer checked the numerical, ownership, datum and binary contracts; root executed the package tests and contract gates. Review is bounded to this query/file layer, not the residual PR121 mission features.

## P1 Assessment

No unresolved blocking source finding remains in this bounded implementation. Full PR-head CI remains a merge requirement.

Unresolved P1: No

## Tests Added/Updated

- Actual-package terrain tests: 336 assertions passed on the final source, Julia 1.12.3, two threads. Tests exercise public exports and doc bindings, a nonsquare analytical plane, centre/edge interpolation, priority and fallback, huge longitude inputs, strict seam boundaries, invalid/narrow grids, array ownership, custom-model extension, conflicting datums and hand-encoded local file fixtures.
- The new example printed in the Concepts guide was executed successfully.
- All 23 unmodified PR contract gates passed, including the actual temporary-index inventory gates and the stale-dependency check. The optional plotting dependency emitted its pre-existing Kaleido startup warning; terrain tests do not plot.
- The new unit file is registered in the standalone runner. Full unit/integration suite execution and Linux PR CI remain pending.
- Documentation build: the unmodified strict build reached document checking after doctests/cross-references, but failed before rendering on four existing external-link DNS failures (curl exit 6). Full documentation acceptance remains required in CI.
- The patch applies cleanly to current main and the pinned heads of PR139 and PR140. This is a source-overlap check, not combined-branch runtime validation.

The source snapshot and temporary Git metadata are removed automatically; the shared checkout, native assets and dependencies are unchanged. The first 335-assertion run predates the final precision regression and was deliberately stopped during the contract gates. It is retained as historical evidence, not used as final acceptance.

## Residual Risk

Full-globe grids, NaN holes/nodata filling, ellipsoidal height conversion, automatic map downloads and scientific registration of mission assets are unsupported here. Grid bounds are outer cell edges, samples are cell centres, edge half-cells clamp to the edge samples, and the first covering grid wins. Known radii must match exactly after Float64 conversion. Normalized western bounds are stored in [0,360), and the eastern bound may exceed 360. Source arrays are copied, but exposed model/grid arrays remain mutable and must be treated as read-only after construction. No allocation, speedup, native-GRAM, propagation or viewer-parity claim is made.


## Documentation CI correction, September18

The initial docs job on `9cb9e5bb` failed because the generated Public API page was208.53KiB against the existing200KiB limit. The documentation-only follow-up partitions the unchanged symbol registry into the main reference, Terrain API and Visualization API, with navigation and reciprocal links. The registry remains complete; renderer defaults remain compatible with existing callers. `docs/make.jl`, `docs/public_api_symbols.jl`, `docs/src/public_api_policy.md` and `docs/src/maintainer/index.md` change; no terrain runtime or test code changes.

The complete local `docs/make.jl` now exits0, including doctests, cross-references, exported-doc checks and HTML rendering. Generated sizes are166.49KiB for Public API,20.52KiB for Terrain API and43.80KiB for Visualization API. The200KiB failure threshold is unchanged. Deployment is skipped by Documenter outside a deployment environment. These results supersede the earlier local DNS-limited build; fresh Linux current-head CI is still required.
