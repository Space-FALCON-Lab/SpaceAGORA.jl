# AI Review Artifact — PR #114

## Scope
Follow-up to #101: the scenario environment filter applies to the CLI path only, typed selections are normalised, and the initial-condition fit requests its target scenario explicitly.

## Changed Files
- src/analysis/verification/telemetry_verification/types.jl — `VerificationRequest.scenarios` defaults to `String[]` (every scenario) instead of parsing `SPACEAGORA_TELEMETRY_SCENARIOS`; the CLI parser keeps the environment fallback.
- src/analysis/verification/telemetry_verification/runner.jl — `_select_scenarios` lowercases, strips and de-duplicates the requested names before comparing them with the lowercased manifest names; blank entries count as no request.
- src/analysis/verification/telemetry_verification/ic_fit.jl — `_ic_fit_run` passes `scenarios=[target]`, so the fit propagates only its target and cannot be narrowed away from it.

## Findings
- P2 (Codex on #101): with the environment filter in the struct default, `fit_initial_state` under `SPACEAGORA_TELEMETRY_SCENARIOS` set to another scenario propagated nothing for its target and `_ic_fit_series` threw. Resolved by the empty default plus the explicit target.
- P2 (Codex on #101): a typed request with `"Odyssey"` was rejected as unknown while `--scenarios=Odyssey` succeeded, because only the CLI parser lowercased. Resolved in the selector.
- P3: behaviour of the CI jobs and the study script is unchanged: they go through `parse_cli`, which still reads the environment variable.

## P1 Assessment
No P1 findings. Unresolved P1: No

## Tests Added/Updated
- test/probes/coverage_parallel_telemetry_probes.jl: under the environment variable a code-built request keeps an empty selection while `parse_cli` keeps the filter; blank entries select everything; a padded upper-case name selects its scenario. The probe passes locally.

## Residual Risk
Low. One default and one normalisation; the fit's request change reduces work rather than adding paths.
