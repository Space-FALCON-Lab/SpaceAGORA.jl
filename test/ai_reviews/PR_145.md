# Plume extraction review

## Scope

Extract Evan's PR121 plume interaction onto terrain branch `6db154cc`, implementation `e486b803`. Codex inspected the force and diagnostic ownership, callback ordering, geometry and construction validation. The implementation agent executed the retained actual-package tests. No Claude review or independent experimental calibration is implied.

## Changed Files

- `src/dynamics/coupled/force_torque_models/plume_surface_interaction.jl`: optional effector, pure force evaluation and accepted-step diagnostic helpers.
- `src/dynamics/coupled/force_torque_models.jl`: canonical include and namespace forwarding.
- `src/simulation/callbacks/plume_callbacks.jl`: accepted-state quadrature and saved-field sampling.
- `src/simulation/callbacks/callbacks.jl`: callback include.
- `src/simulation/callbacks/registry.jl`: callback registration after control and attitude updates.
- `src/simulation/callbacks/density_callbacks/assembly.jl`: callback assembly.
- `src/simulation/callbacks/save_fields.jl`: optional plume diagnostics.
- `src/SpaceAGORA.jl`: public API forwarding.
- `docs/public_api_symbols.jl`, `docs/make.jl` and `docs/src/user/plume_interaction.md`: API registration and assumptions, units, limitations and usage.
- `test/unit/environment/plume_surface_interaction_tests.jl` and `test/unit/runtests.jl`: registered geometry and real engine acceptance tests.

## Findings

No blocking source finding in the bounded effector. Both wrench paths leave diagnostic state unchanged. The accepted-step callback integrates each preceding interval with its held thrust, then stores the new endpoint command. Dense saves use the corresponding accepted interval. A new run resets diagnostics; checkpoints within one run retain them. Existing backbone eligibility rejects this effector before initialization.

## P1 Assessment

The previous RHS-stage history risk is removed. Tests include reordered calls, actual rejected steps, independently computed mass across a thrust jump and saved samples. Terrain uses planetocentric coordinates and its declared sphere. Upward or horizontal exhaust does not invent a surface impact. The collinear nozzle offset and axial augmentation imply zero moment about the integrated reference point.

## Tests Added/Updated

128 actual-package assertions passed: construction, geometry and pure interfaces; two-spacecraft engine execution, accepted-step/save oracles, isolation, repeated runs, four checkpoint segments, positive propagated augmentation and unsupported-backbone rejection; and refinement against independent time quadrature. All 23 contract gates and strict documentation passed. The API page was 176.34 KiB under the unchanged 200 KiB limit. The original import failure from doc forwarding is retained; it ran no tests and was corrected before this successful run.

## Residual Risk

This is a phenomenological closure with illustrative parameters, not calibrated flight physics. Local tangent-plane geometry omits ray tracing, slopes and gas/soil transport. Eroded mass is a step-size-dependent diagnostic, not an error-controlled ODE state. A separately resumed run starts a new integral. Full Linux CI, combined landing-controller execution and performance measurements remain pending. This PR depends on #141 and must be retargeted and checked against main after terrain merges.
