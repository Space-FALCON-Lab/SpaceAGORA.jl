# AI Review Artifact: Configuration preservation in smoke runs

Reviewed implementation: `0e490021`; baseline: `4b42b544`.

## Scope

Preserve explicitly configured solvers when creating smoke configurations. Add one private field-preserving helper and migrate only the smoke builder. The helper performs a shallow update; runtime isolation remains with the engine's default deep copy. Public exports, engine solver precedence and other configuration rebuild sites are unchanged.

Claude independently executed the focused probe and selected gates, checked the diff and ran CYGNSS in smoke mode. Codex checked the retained review evidence against the unchanged source commit.

## Changed Files

- `src/core/state/simulation_configuration.jl`: add `_with_configuration`, preserving all unspecified fields by reference and re-inferring model types through the constructor after named overrides.
- `src/analysis/verification/telemetry_verification/example_support.jl`: replace the smoke builder's incomplete field list with that helper, overriding only the existing smoke settings and mission configuration.
- `test/probes/configuration_copy_probes.jl`: add preservation, override, type, error, real-solver and isolation regression checks.
- `test/suites/09_probe_drivers.jl`: register the new probe.

## Findings

No blocking findings remain. The baseline smoke copy dropped a typed Rodas5P setting and an actual simulation selected Tsit5 from the environment. The corrected copy preserves the typed solver; explicitly clearing it still selects the environment solver. CYGNSS smoke now executes its configured Rodas5P, confirmed by its solver trace and successful return code.

Existing smoke behavior is preserved: mission and sample caps, output location, environment-controlled results and CSV output, disabled plotting and normalization, and reset checkpoint fields. Replacement models may change the configuration's parametric types; unknown keywords retain the constructor's error. No additional deep copy is introduced.

## P1 Assessment

No P1 issue was identified in this bounded change. Engine routing, numerical kernels and public exports are unchanged. Preserving the requested solver deliberately changes the smoke solver for configurations previously affected by the omission.

## Tests Added/Updated

The new probe passes all 101 assertions, including two actual short simulations under a conflicting environment, explicit solver clearing, replacement models, shallow reference preservation, both smoke output settings and source-frame isolation after execution. Claude independently repeated those assertions. All 23 PR contract gates pass locally; the existing Earth no-GRAM beginner example and CYGNSS example complete bounded smoke runs.

## Residual Risk

Full integration, documentation build and Linux CI on this PR head remain pending. This review does not certify full-duration campaigns or performance. Checkpoint resets are existing smoke behavior, now pinned by tests. Other reconstruction sites, telemetry-specific precedence and calibration environment isolation remain separate work. The private helper preserves references by design and relies on the engine's normal isolation behavior for execution.
