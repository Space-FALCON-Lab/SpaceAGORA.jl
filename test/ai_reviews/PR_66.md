# AI Review — PR 66: study README rerun after the dipole-field sign fix

## Scope
Documentation-only: the CYGNSS attitude study's "Independent-effect results" table recorded numbers produced with the pre-#64 antiparallel tilted-dipole field. Full rerun of `run_independent_effects.jl` on post-#64 main (same script, same telemetry, GRAM per-link aero live).

## Changed Files
- `benchmarks/studies/cygnss_attitude_torque_reconstruction/README.md` — independent-effects table and interpretation updated with the fixed-field rerun; the pre-fix numbers are preserved inline for provenance.

## Findings
- A/B controls: the wheel-only, gravity-gradient, and aero rows reproduce the pre-fix table to the last digit (the sign fix cannot touch them), so the magnetic-row change is attributable to the field sign alone.
- The magnetic row flips from degrading (+2.82 deg) to helping (−6.3 deg), and the all-combined row from +14.05 deg worse to −18.4 deg better — resolving the study's standing "magnetic makes it worse" anomaly as the field-sign bug found in PR #64.
- The full combination still does not beat gravity-gradient alone, consistent with the documented dplCmd unit/geometry approximations and the ill-posedness of hour-scale open-loop replay.

## P1 Assessment
No P1 findings. No code changes.
Unresolved P1: No

## Tests Added/Updated
- None (documentation); the study regeneration itself is the evidence, and its plot-data arrows were rewritten in place.

## Residual Risk
- The dplCmd duty-to-moment conversion and rod DCMs remain face-value in the public study (correct treatment lives in the private campaign); a future pass could port them.
