# PR #150: Terrain imagery and export review

## Scope

Integrate the Moon terrain producer, canonical DEM export and regional viewer rendering from PR121. Reviewed candidate e804b9ee was independently accepted by Claude with retained browser checks. Follow-up4599d10f removes redundant grayscale conversion arguments and clarifies two guide points. No propagation, atmosphere or touchdown physics changes. This numbered artifact records the published terrain package. Current integration validation is recorded below.

## Changed Files

- `src/analysis/visualization/scene/scene_visualization.jl`: forward the optional local terrain export.
- `src/analysis/visualization/scene/terrain_payload.jl`: canonical loader, explicit datum and edges, reduced centre sampling, bounded portable assets.
- `src/analysis/visualization/scene/viewer_bundle.jl`: embed terrain metadata and local assets with the viewer.
- Python terrain tools, requirements, viewer terrain/globe/main modules, documentation, registered tests and CI steps complete the integration. See the retained18-file manifest for the full inventory.

## Findings

No blocking source or browser finding on the reviewed candidate. Longitude seam aliases and singleton grid axes follow the same centre/edge convention in the producer, Julia exporter and JavaScript sampler. Cache identity and contained paths are validated. Imagery replacement preserves previous files on failure. Bounds retain deep and raised terrain; the globe cutout uses normalized coordinates. Follow-up image modes are inferred from two-dimensional uint8 arrays with unchanged pixels and encoded PNGs in the executed comparison.

## P1 Assessment

The package keeps the simulation's terrain separate from a possibly reduced visualization grid and states radial clearance explicitly. Native GRAM is not required. Browser tests verified the bounded fixture's terrain clearance and cutout in both display frames. These checks do not establish full-resolution imagery registration, physical contact dynamics or a complete landing mission.

## Tests Added/Updated

Original integration:1115 focused Julia assertions,38 actual producer/export bridge assertions,23 contract gates,15 Python cases,11 existing viewer JavaScript cases,9 terrain payloads and117 height queries, and strict documentation. Claude independently reran109 exporter and336 terrain assertions,15 Python cases,11 viewer JavaScript cases and8 payloads/101 queries, plus independent sampler checks and browser acceptance. Counts are separate runs, not additive coverage claims.

Two-file follow-up:15 existing Python cases pass with deprecation warnings treated as errors. Before/after execution covers all five changed conversions through actual producer functions and yields identical modes, dimensions, pixels, PNG encodings and numeric tone parameters. Local versions are Pillow10.2.0 and NumPy1.23.2. No future Pillow release was executed. Renderer and Julia source files remain byte-identical to the accepted candidate.

## Residual Risk

The prepared integration onto refreshed PR148 has passed focused tests and strict documentation, including the corrected guide. Its actual merge parent and numbered artifact gate are verified when published. Complete Linux CI remains required. Live NAC acquisition, renewed high-resolution registration, full real-DEM landing, GPU performance and physical calibration remain unverified. Inherited imagery registration constants are configured values, not new measurements. The official Pillow policy does not support the earlier blanket Pillow13 failure prediction for these dtype-preserving grayscale calls; no dependency cap was introduced.

## Refreshed viewer integration

The latest refresh incorporates accepted main `29e6d92d` and the repaired viewer. All eighteen terrain feature paths are retained; seventeen retain the exact earlier implementation, while the unit registry additionally preserves the plume registration. All 46 registrations occur once. Every other tracked path preserves the viewer, including the test-only performance-canary repair.

Executed on `56931e653486170e46597849ebc69a31c4b5152d` (tree `679aae9f34516d733cbd53f79fad24123b4cbf80`): 1,115 focused Julia assertions, all 23 contract gates, 15 Python cases with deprecation warnings treated as errors, 11 viewer JavaScript cases, eight Julia-exported payloads and 101 terrain queries, and strict documentation with external links. Source hashes remain unchanged. Native GRAM was not used. The final prepared candidate differs only in PR148's updated review artifact; runtime, tests, documentation, producers and renderer inputs remain identical. Publication is based on verified viewer merge `831a3b2dedfbfe48f9a0dafdfa6b5128fd956c08`. The feature commit `c5f7fae87d6b8a625b775b8eda4c88786ab6ad2a` preserves the exact prepared tree `a7c8807c2a9eddea1dfda186a824c5c8d9e1e9ba`. The next commit adds only this numbered artifact. The artifact gate is checked on that committed tree; fresh Linux CI is required.

Original producer-bridge, small live LOLA acquisition and browser results remain separate historical evidence. They were not rerun for this integration. No new full-resolution imagery, complete landing or physical-calibration claim is made.

Unresolved P1: No

## Integration with accepted gravity, Apollo and mission packages

The September 19 refresh integrates main `1ce7b22f14d9af1bda5a5b54f409adc923729934`. The integration commit is `6aae7815afc2189f9f5a41f81d1f964a9d2e09fd`, tree `108e92c37b937cbc327234781ab16482d279cbd7`. The terrain feature paths preserve the prior reviewed implementation; the unit runner preserves all 49 registrations on current main and adds the terrain exporter once, for 50 unique registrations. Other tracked paths preserve current main.

Fresh execution on this integrated tree: 1,591 focused Julia assertions spanning terrain, ensemble, viewer, demo options, gravity torque, Apollo, touchdown and mission helpers; all 23 contract gates; 15 Python producer cases with deprecation warnings treated as errors; 11 viewer JavaScript cases; eight exported payloads and 101 terrain queries; and strict documentation with external links. The numbered artifact gate passes. Source hashes are unchanged during validation. The final follow-up adds only this review record and reruns the artifact gate on the committed tree. Full Linux CI on the published head remains required.

Two local harness failures are retained: an initial snapshot omitted tracked 8k textures, and the first documentation invocation lacked the temporary repository's origin metadata. Restoring the exact tracked assets and supplying the temporary Git metadata resolved those failures without product changes. Native GRAM was not used. Browser and live acquisition evidence are the earlier independent runs, not repeated claims.
