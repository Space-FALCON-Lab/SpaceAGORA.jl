# AI Review Artifact: Initial-condition altitude correction

Reviewed implementation: `43b2133d`; sandbox correction: `d1ebfff0`; baseline: `4b42b544`.

## Scope

Correct the planet-aware initial-condition constructor so the initial ellipse recovers the requested geodetic apsis altitudes. Share the previously correct guidance arithmetic through a private `Geodesy` module while preserving public inputs, frame precedence, error policies and existing guidance numerics. Runtime frame conversion, routing and atmosphere models are unchanged.

Claude independently reviewed the implementation, sandbox correction and retained native-example evidence. Codex reviewed the follow-up test changes and retained execution results.

## Changed Files

- `src/core/numerics/geodesy.jl`: shared altitude, surface-radius and altitude-to-radius calculations.
- `src/core/simulation_model.jl`: include the shared module before its consumers.
- `src/gnc/guidance/guidance_hooks.jl`: import the shared functions.
- `src/gnc/guidance/thruster_guidance/thruster_guidance_functions.jl`: retain existing helpers as delegates.
- `src/vehicle/spacecraft/model.jl`: use shared geometry with existing constructor validation.

Tests update the constructor probe, suite-09 registration, integration sandbox and suite-01 guidance regression.

## Findings

- **P1, addressed:** an extra sine factor made constructor altitude calculations inconsistent with guidance and runtime geometry. Independent geometry checks, example pins and propagation checks support the correction.
- **P2, addressed:** raw inclusion of the guidance file left shared delegates unresolved in the test sandbox. Matching the production imports and exercising the helpers and periapsis-raise command fixes that regression.
- **Intentional behavior change:** corrected initial altitudes can change drag exposure and altitude-dependent termination in affected examples. Defective initial values are not retained as regression targets.
- **Evidence boundary:** unchanged Odyssey results establish compatibility of its Cartesian-state and delta-v-table path. Separate constructor and guidance checks cover the changed paths.

## P1 Assessment

No remaining P1 source defect was identified within this scope. The constructor defect and sandbox regression are addressed. Linux CI on the published PR head remains required.

Unresolved P1: No

## Tests Added/Updated

- Constructor probe: 268 assertions covering hemispheres, poles, spherical and oblate bodies, frame precedence and compatibility against independent geometry.
- Guidance sandbox: nine assertions, including a prograde periapsis-raise command checked against an independent equatorial vis-viva calculation.
- Existing guidance/reference probes, all 27 contract gates, example initial-condition pins, and inverse-square/J2 propagation checks pass. The J2 check distinguishes sampled trajectory minima from initial ellipse geometry.
- Before/after frame and guidance values retain identical bits. Odyssey quick enforcement and scientific table comparisons pass on its unaffected path.
- Bounded native runs complete three periapsis passages for each Mars orientation and a real Earth periapsis followed by the original orbit-count termination. The first Earth harness attempt was rejected because its temporary bound expired at the starting apoapsis; the corrected bound verifies an actual orbital passage without changing production callbacks.
- With separate test-only branch `96dd1765` applied to `d1ebfff0`, the complete policy file, standalone unit tree and full integration suite pass through suite 10. Three topology skips and two pre-existing RPO broken markers remain. The altitude branch alone retains the pre-existing resource-test failures corrected by that separate branch.
- The numbered artifact gate and Linux PR CI remain pending publication.

## Residual Risk

Affected example trajectories intentionally change. The full local pass covers the combined altitude and test-only tree; it does not replace CI on the published heads. Bounded native runs use the recorded local GRAM installation, whose wrapper differs from the repository gitlink. They do not resolve the installation mismatch or establish fresh-clone or full-duration campaign results.

The Mars apoapsis keyword interpretation, output-column semantics and runtime frame ownership remain outside this correction. Numerical tables and telemetry evidence remain private. No performance improvement is claimed.
