Home for function-level unit tests migrated out of `test/suites/03_persistence_units_and_rotational_tests.jl` (persistence/results-bundle unit cases), mirroring `src/io/`.

See `test/TEST_RESTRUCTURE_PLAN.md` for the migration order and `test/README.md` for the convention new tests here should follow.
