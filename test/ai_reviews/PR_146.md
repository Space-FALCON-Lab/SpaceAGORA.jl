# Apollo descent and terrain-touchdown integration review

## Scope

Refresh Evan's Apollo-inspired guidance/control and terrain-touchdown extraction from `921976b1dd4060c39783ea4453d56772d8589bfb` onto actual main `29e6d92d216012779a40a9c1b2a2f940f3b2d59e`. The terrain and plume dependencies are already on main. This PR now integrates main directly. The only conflict was the unit runner. Starting from main's complete list, touchdown and Apollo tests are added after the direct-assembly test, retaining all 46 registrations and avoiding the pending viewer insertion point. The existing shared CI canary correction from viewer head `af8f3abe` is copied exactly; no runtime calibration or routing behavior changes.

## Changed Files

- `src/gnc/control/control_hooks.jl`: touchdown extension hook and Apollo control include/export.
- `src/gnc/control/landing/apollo_descent_control.jl`: bounded engine/attitude control, approximate mass flow and contact cleanup.
- `src/gnc/guidance/guidance_hooks.jl`: shared-math imports and Apollo guidance include/export.
- `src/gnc/guidance/landing/apollo_descent_guidance.jl`: explicit-datum site geometry, braking/approach/vertical guidance and shared state.
- `src/simulation/callbacks/registry.jl`: terrain and touchdown-hook imports, preserving plume imports.
- `src/simulation/callbacks/event_callbacks.jl`: validated per-index touchdown and opt-out from the ordinary impact stop for selected craft.
- `src/simulation/callbacks/density_callbacks/assembly.jl`: touchdown assembly alongside existing frame/plume callbacks.
- `test/unit/gnc/apollo_descent_tests.jl`, `test/unit/simulation/touchdown_tests.jl`, `test/unit/runtests.jl`: focused actual-engine regressions and normal test registration.
- `docs/src/user/stop_conditions.md`: touchdown extension contract, explicit radius, controller pairing and model limits.
- `test/unit/parallel/cost_machine_calibration_tests.jl`: exact already-reviewed timing-test correction from PR148; production timing code and thresholds unchanged.

## Findings

Touchdown uses run indices, validates selections and DEM datum consistency, preserves the ordinary impact stop for unselected craft and deactivates only selected craft. Control, guidance and force/mass-flow callbacks respect inactive state. Existing tests discriminate spacecraft IDs from run indices, preserve aliasing through isolation and check command cleanup. All seven runtime files, both focused tests, the guide and plume implementation remain identical to the reviewed prior head. The earlier correctly sized thruster arithmetic is unchanged; undersized buffers keep the reviewed BoundsError fix.

Independent source preservation review found no integration blocker. The canary test checks each real sample's finite positive measurement, signed relative drift and the unchanged inclusive 25% decision. It does not assume successive timings agree; there are no retries, skips or widened production tolerances. The pending viewer integration merges cleanly at the reviewed heads. Static merge compatibility is distinct from combined runtime validation.

## P1 Assessment

No source or integration P1 finding in the reviewed refresh. Fresh current-head Linux CI remains required before merge.

Unresolved P1: No

## Tests Added/Updated

Executed on `a2094144284fd5a78e90eaca6dc2c61025db704f`, tree `c78e29726275e2c532a0e9e348dbb2adc42ebc37`, Julia 1.12.3 with two threads: 105 apollo descent, 34 touchdown, 147 plume surface interaction, 60 cost machine calibration; 346 focused assertions total. All 23 contract gates, the numbered artifact gate and strict documentation including external links pass. Both runners exit zero and consumed source hashes remain unchanged. Fresh focused coverage measures 150/150 executable control-file lines (100.00%), above the unchanged 80% floor. This is per-file local coverage; the full repository coverage gate still requires fresh Linux CI.

The final follow-up updates only this review record. The artifact gate is rerun on that final commit; runtime, tests and user documentation match the executed candidate exactly. No native GRAM was requested. The earlier all-phases descent (20 checks) and combined landing (32 checks) remain separate retained evidence on unchanged feature source. They were not rerun here. This new documentation build includes strict external-link checks; older DNS-limited documentation runs remain historical evidence only.

## Residual Risk

This is Apollo-inspired configurable guidance, not a historically calibrated reconstruction. Body torque and RCS propellant accounting are approximate; per-jet levels are visualization diagnostics. Contact deactivates the craft and holds inertial state, rather than modeling body-fixed rest, bounce or contact forces. The plume closure remains phenomenological. Full unit/integration/nightly suites, native-SPICE landing, physical calibration and performance measurements were not rerun locally. These local results are not a substitute for the published head's CI. Main or PR-head changes require checking the new combination before merge. The viewer/terrain publication sequence remains first; this refresh does not authorize bypassing that sequence or its checks.
