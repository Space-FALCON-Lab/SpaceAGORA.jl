# AI Review Artifact for PR #105

## Scope
Performance default in the effector-threading policy: a single satellite is evaluated serially unless `SPACEAGORA_EFFECTOR_PARALLEL=on`. No numerical change (threaded and serial are bit-identical since #103).

## Changed Files
- src/simulation/engine/setup.jl — early serial return in `_dynamic_effector_thread_decision` when `num_sats <= 1` and the mode is not `:on`.

## Findings
- P2: the rule must not disable threading for constellations or for users who force it. Checked: the return triggers only for `num_sats <= 1` and `mode != :on`; two satellites under auto still thread, one satellite under `on` still threads (suite 04 cases updated to cover all three). Resolved.
- P3: `policy_applied=false` on the early return means no cost sample is recorded for single-satellite runs; the cost model is only used by the multi-satellite plan selection, so nothing depends on it. Noted.

## P1 Assessment
No P1 findings. Unresolved P1: No

## Tests Added/Updated
- test/suites/04_solver_env_and_regression_tests.jl — single satellite under auto is serial; `on` threads; two satellites under auto thread.
- Manual: decision cases replicated locally; contract gates; threaded smoke incl. the bit-identity check.

## Residual Risk
Low. A user with a single satellite and genuinely heavy effectors loses the automatic threading and must set `SPACEAGORA_EFFECTOR_PARALLEL=on`; documented in the parallel execution guide.
