# Gravity-gradient extraction review

## Scope

Extract the standalone central-field torque effector from Evan's PR121 (`cc891c76`) onto main `f0ccc21d`. Implementation commit `5e626a57`. Codex reviewed the source and executed the candidate against independent vector mathematics and actual engine propagation. No external-review approval is implied.

## Changed Files

- `src/environment/gravity/gravity_models.jl`: adds the opt-in effector and delegates both supported interfaces to the existing torque owner.
- `src/dynamics/coupled/force_torque_models/gravity_effectors.jl`: owner export.
- `src/dynamics/coupled/force_torque_models.jl`: aggregate import/export.
- `src/environment/gravity/gravity_effectors.jl`: environment namespace forwarding.
- `test/unit/environment/gravity_gradient_torque_tests.jl`: independent vector-law, real RHS and propagation regressions.
- `test/unit/runtests.jl`: registers the test.
- `docs/src/user/custom_effector.md`: usage, assumptions and double-counting caveat.

## Findings

No blocking finding in the reviewed extraction. Both interfaces reuse the existing central-field torque law and return exactly zero translational force. Run selection follows index, demonstrated with IDs 41 and 97. Missing attitude, missing spacecraft, invalid index and degenerate position follow the existing owner's behavior. No automatic threading capability is newly declared. The explicit solver partition is unchanged.

## P1 Assessment

No P1 issue identified. The independent body-frame vector formula checks the scalar-last inertial-to-body convention. Actual engine tests distinguish a missing torque and compare rotational acceleration including the body's gyroscopic contribution exactly once. A separate gravity force plus this torque agrees with the built-in analytic-gravity torque option.

## Tests Added/Updated

Executed on the actual candidate package with Julia 1.12.3 and two threads: 198 focused assertions across pure interface, engine and three 20-second attitude propagations; all 23 repository contract gates pass. Trajectory comparisons use explicit absolute tolerances, not an unmeasured bit-identity claim. Tests include source-state isolation and disabled attitude/disabled torque behavior. No native GRAM was requested.

## Residual Risk

The effector is a central-field approximation. Enabling this and an existing gravity-gradient option together double-counts torque; documented deliberately. The local strict docs run reached internal-reference validation but external links failed sandbox DNS; unchanged network-enabled rerun is pending. Full integration and Linux CI, combined runtime with other PR121 extractions and performance measurements are not claimed here. The normal CI suite must pass on the published head before merge.
