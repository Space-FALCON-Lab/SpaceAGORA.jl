name: Julia CI

on:
  pull_request:
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - run: echo "seed check: tests"

  coverage-quality-gate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "seed check: coverage-quality-gate"

  p1-findings-gate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "seed check: p1-findings-gate"

  ai-review-artifact-gate:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - run: echo "seed check: ai-review-artifact-gate"
