# AI Review — PR #34

## Scope
- Reviewed the fixes for all 11 bugs from PR #33's inventory plus the twelfth found during the campaign: the `rtolatlong` Bowring geodetic-altitude defect (the propagation-side twin of the thruster-guidance copy), which under-reported altitude everywhere off the equator/north pole and materially over-estimated drag density at mid/high latitudes.
- Review method: every fix verified by its updated probe file under `--depwarn=error --threads=2`; full integration suite exit 0 with zero failures including the golden regression tests; and a measured telemetry A/B against the GRAM-deterministic study with the native GRAM library — pristine main reproduces the failing CI rows closely; the fixed tree passes the earth_gmat apoapsis thresholds, materially improves Odyssey, and leaves VEX bit-identical exactly as the zero-flattening analysis predicted (quantitative results recorded in internal lab notes).
- Diff forensics on suspect commits e0015233 and 8d7e0f84 exonerated both for the telemetry rows (the atmosphere fast-path guard never executes under the harness's solver modes).

## Changed Files
- `src/core/interfaces/reference_system.jl` — Bowring altitude closed form corrected (`e²·N·sinφ` term); `latlongtor` prime-vertical radius sqrt and z-term; `latlongtoOE` Float64 mass; topography branch resolves the includer-module `args` global when present (suite-03 sandbox contract) instead of throwing UndefVarError.
- `src/gnc/guidance/thruster_guidance/thruster_guidance_functions.jl` — same Bowring correction in `_oblate_altitude_from_radius`.
- `src/environment/gravity/gravity_models.jl` — J2 acceleration uses the equatorial radius `Rp_e`, consistent with `j2_secular_rates` and the harmonics path.
- `src/simulation/callbacks/density_callbacks/vacuum_predicted_gram.jl` — same J2 radius fix in `_vacuum_j2_accel`.
- `src/gnc/navigation/rpo_nav/distances/clearance.jl` — `rpo_path_clearance_stats` honors `safe_distance_m`.
- `src/gnc/guidance/rpo/hypr_planning/replanning.jl` — retimed plans report the retime instant in `planned_at_s` (merge order).
- `src/gnc/guidance/rpo/hypr/pso_path_planning.jl` — truthful timeout phase when re-exploration is disabled; failed stagnation-learning attempts no longer reset the stagnation counter (helper `rpo_pso_stagnation_count_after_learning`).
- `src/gnc/robotics/robot_arm_hypr/planner_core.jl` — `q_start` length validation runs before IK.
- `src/parallel/policy/context.jl` — spin-barrier pool constructor clamps workers to `nthreads()-1`; per-worker error slots replace the racy shared-Ref read-modify-write.
- `src/parallel/policy/types.jl` — `_SpinBarrierPool` gains the per-worker `errors` vector.
- `src/simulation/callbacks/density_callbacks/config.jl` — `_gram_track_trajectory_supported` probes Module drivers with `isdefined` (hasproperty only sees exported names).

## Findings
- P1 (fixed): the `rtolatlong` altitude defect corrupted the drag density lookup on every Earth/Mars atmospheric pass and is the verified primary cause of the earth_gmat and Odyssey telemetry failures, including Odyssey's impact termination at orbit 12 (over-drag plus the historical periapsis-lowering burns at orbits 7/10/12).
- P2 (fixed): J2 radius inconsistency (systematic understatement); spin-barrier deadlock hazard and error-reporting race.
- P3 (fixed): the remaining inventory items — diagnostics, validation ordering, dead-path wiring.
- Remaining (tracked, not in this PR): Odyssey residual over-drag/corridor crash; VEX apoapsis error, with the polyfit-density-above-EI lead documented in the PR body; `examples/AGORA_Odyssey.jl` persistent ENV mutation.

## P1 Assessment
- The rtolatlong defect was the P1 candidate; it is fixed and its impact verified.
- Unresolved P1: No

## Tests Added/Updated
- All pinning assertions across the nine coverage-debt probe files flipped to assert corrected behavior (the probe files annotate which assertions pin vs survive).
- New regression guards: geodetic altitude round-trips at three latitudes (`atol=1e-3` m); ellipsoid-surface altitude near zero at mid-latitude; stagnation-counter semantics; spin-pool upper clamp; dual-failure error propagation; production-shaped trajectory fast-path support checks (no type piracy).
- Full suite and telemetry A/B as verification, described in Scope.

## Residual Risk
- The physics fixes change trajectories for mid/high-latitude atmospheric passes and J2-dominated propagation; the golden regression tests pass unchanged, but downstream studies calibrated against the old behavior (e.g. the Odyssey Cr calibration, which shifted in the A/B) may need recalibration.
- Two telemetry scenarios remain over threshold with documented mechanistic leads; the investigation continues under the existing tracking.
