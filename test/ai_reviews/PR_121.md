# AI Review Artifact — PR #121

## Scope
Post-hoc interactive 3D visualization of runs. A run flagged with `simulation_settings.save_visualization_scene` (or `run_simulation(args; visualization=true)`, `SPACEAGORA_VISUALIZATION=1`, `spaceagora run --visualize`) writes a scene sidecar beside its results and three new save fields (link poses, arm poses, density); `export_visualization` then bundles the sidecar, the decimated trajectory, a planet texture, optional 3D models (STL/OBJ/glTF), planned-path overlays and reference "ghost" trajectories into one self-contained HTML page driven by a vendored three.js viewer under top-level `viewer/`. Monte Carlo ensembles get their own manifest and page. The last commits add NASA models for Magellan, Cassini-Huygens and the Apollo lunar module, the ghost feature, and mission-recreation drivers (Magellan aerobraking at Venus, Cassini's TA and T5 Titan flybys with the reconstructed SPICE trajectory drawn as a ghost, Apollo 11 in lunar orbit). The final commits add mesh aerodynamics: a free-molecular panel method over a CAD mesh with depth-buffer self-shadowing, an analytic surrogate (real spherical harmonics in the airspeed direction times a polynomial in the inverse speed ratio, with the wall temperature kept as a runtime parameter through the exact sqrt(Tw/T) split), and an `AerodynamicCoefficientMeshSurrogate` effector; the mesh readers move from the visualization scene into the structure layer so both consumers share them. The last round adds a standalone viewer page that builds its payload in the browser from a CSV or JSON table (no simulation, no Julia), a nominal sample and a 3-sigma dispersion tube for ensembles, and fixes a pre-existing defect found while debugging the Magellan case: a GRAM density model carries the epoch it was built with and nothing propagated the run's `initial_time` into it, so local solar time and season were evaluated at 2000-01-01 for every example that built the model without an explicit epoch. Two further additions: model articulations (`articulate_triangles`/`articulation_payload` in the structure layer, `model_articulations` in the bundler, the same rotation applied to the drawn model in `lod.js`), used to turn the Magellan model's cruise-canted wings broadside for both the aerodynamic mesh and the picture; and an MP4 export in the viewer (`video.js`: deterministic frame-by-frame rendering through `viewer.renderAt`, WebCodecs H.264, the vendored MIT `mp4-muxer`, WebM fallback). Reviewed: every changed `src/**/*.jl` against the default no-visualization path, the viewer modules, the bundler's data contract, the CDN page builder used for artifact pages, and the new unit tests. Rendering was verified in headless Chromium (Playwright, SwiftShader) on this machine for the ISS, Magellan, Cassini and Apollo pages, both the local `data:`-URL variant and the CDN variant.

## Changed Files
- src/SpaceAGORA.jl — imports and re-exports the `SceneVisualization` API (`VisualizationScene`, `build_visualization_scene`, `export_visualization`, `with_visualization_scene`, `sample_model_pointcloud`, `load_model_triangles`, `model_bounding_box`, ensemble helpers, `run_monte_carlo_visualization`, ...) with `@doc` forwarding. Additive.
- src/core/simulation_model.jl — includes `analysis/visualization/scene/scene_visualization.jl` and re-exports it before the callbacks include (the callbacks read the link-pose helpers). Aggregator-only change, allowed for this file.
- src/core/state/simulation_configuration.jl — `SimulationSettings.save_visualization_scene::Bool = false`.
- src/simulation/engine/execution.jl — `run_simulation(args; visualization=...)` defaults from `SPACEAGORA_VISUALIZATION` through the existing `_engine_env_get` adapter; when set it rebuilds `args` with `with_visualization_scene` before the deep copy (caller's config untouched), appends the visualization save fields to an explicit `save_fields` list, writes the sidecar after results and builds the page. With the flag off `visualization_save_fields` is empty and both hooks short-circuit.
- src/simulation/engine/persistence.jl — `_write_visualization_scene_if_enabled!`, gated on `results` and the flag.
- src/simulation/callbacks/save_fields.jl — `:link_pose` (7 floats per non-root link), `:arm_pose`, `:density` save fields and `visualization_save_fields`; `default_save_fields` appends them only when the flag is on.
- src/simulation/callbacks/registry.jl — one `using ..SceneVisualization` line.
- src/simulation/campaigns/constellation_ensemble.jl — ensemble member settings propagate `save_visualization_scene`.
- src/simulation/campaigns/simulation_campaigns.jl, src/simulation/campaigns/monte_carlo_visualization.jl — `run_monte_carlo_visualization(build_args, seeds, dir; ...)`: one flagged run per seed into `sample_NNNN/`, a manifest, the ensemble page.
- src/cli/spaceagora_cli.jl — `run --visualize` (sets the env switch for the child) and the `visualize` subcommand (`--run`, `--out`, `--max-frames`, `--frame`, `--texture`, `--trail-orbits`, `--title`, `--no-textures`, `--model`, `--model-scale`, `--model-center`, `--ensemble`).
- src/analysis/visualization/scene/scene_visualization.jl — the `SceneVisualization` module: geometry/spec types, schema version, includes of the files below.
- src/analysis/visualization/scene/planet_spec.jl — planet spec and the sampled J2000-to-body rotation table (sign-continuous quaternions from `planet_frame_lpi`).
- src/analysis/visualization/scene/atmosphere_spec.jl — density profile and lat/lon map sampled from the run's density model under the GRAM lock.
- src/analysis/visualization/scene/spacecraft_geometry.jl — link boxes, thruster/facet glyphs, arm geometry from a `RobotArmPlan`, link/arm pose vectors, `velocity_aligned_quaternion`.
- src/analysis/visualization/scene/scene_export.jl — scene build, JSON round trip with schema check, atomic write, `with_visualization_scene`, frame budget.
- src/analysis/visualization/scene/model_geometry.jl — STL/OBJ/glTF triangle readers, bounding box, area-weighted point clouds for planners (introduced mid-branch, then moved to `src/vehicle/structure/mesh_geometry.jl` and deleted in the final commits).
- src/analysis/visualization/scene/ensemble_export.jl — ensemble samples, manifest, resampling onto a shared time axis, ensemble frames and page.
- src/vehicle/structure/mesh_geometry.jl (new) and src/vehicle/structure/structure_models.jl — `articulate_triangles` (rotate the vertices inside an axis-aligned region about an axis through a pivot, in model units) and `articulation_payload`, `load_model_triangles(...; articulations)`; the STL/OBJ/glTF triangle readers, `model_format`, `gltf_required_extensions`, bounding box and point-cloud sampling now live in `Structure` (with `JSON`, `Base64`, `Random` and `Kinematics.rot`); `SceneVisualization` imports and re-exports the same names, so root exports are unchanged. The former src/analysis/visualization/scene/model_geometry.jl is deleted.
- src/dynamics/coupled/aerodynamic_mesh_surrogate.jl (new, inside `AerodynamicEffectors`) — `MeshAeroPanels`/`mesh_aero_panels` (`articulations` pass through to the reader; facet centroids, winding normals, areas, normalisation defaults), `panel_shadow_mask` (depth buffer on the plane normal to the flow), `_schaaf_chambre` per-facet pressure/shear split into the `A + sqrt(Tw/T) B` parts, `panel_aero_coefficients(_split)`, `panel_projected_area`, the real spherical-harmonic basis and Fibonacci directions, `MeshAeroSurrogate`/`fit_mesh_aero_surrogate`/`mesh_aero_coefficients` with JSON I/O, and the effector with `wrench`, `wrench_caching!`, `calcForceTorque`, `environment_requirements` and `solver_partition`.
- src/dynamics/coupled/force_torque_models/aerodynamic_effectors.jl, src/dynamics/coupled/force_torque_models.jl — include and export the new names.
- src/simulation/callbacks/density_callbacks/assembly.jl, src/simulation/callbacks/registry.jl — the new effector counts as an atmospheric effector so the density callbacks are staged for it.
- src/simulation/engine/setup.jl, src/simulation/engine/dynamics_rhs.jl — the new effector is thread-safe, counts as aero for the effector-heterogeneity decision, and carries the same cost rank as the fM model.
- src/SpaceAGORA.jl — root exports and docstring forwarding for the mesh aerodynamics API; `load_model_triangles` docs now forward from `Structure`.
- src/analysis/visualization/scene/scene_visualization.jl — imports the readers from `Structure`; the include of the deleted file is gone.
- src/environment/atmosphere/density_models.jl, src/environment/physical_models.jl — `GRAMAtmosphereModel` gains `constructor_kwargs` (the keywords it was built with; the property forwarding to the core lists the new field) and the generic `with_density_model_epoch(model, initial_time) = model`, exported from `EnvironmentModels`; the GRAM extension (ext/SpaceAGORAGRAMSuiteExt.jl) stores the keywords, keeps them through its deepcopy path, and rebuilds a GRAM model (and a surrogate's base) at the run epoch when the epochs differ.
- src/simulation/engine/execution.jl — `_with_density_model_epoch(args)` runs before the isolating deep copy in `run_simulation`: identity for models without an epoch, otherwise a rebuilt `EnvironmentModel`/`SimulationConfiguration` with one `@info`. Non-GRAM runs are bit-identical.
- src/analysis/visualization/scene/ensemble_export.jl, src/simulation/campaigns/monte_carlo_visualization.jl, src/analysis/visualization/scene/scene_visualization.jl — the ensemble manifest records a `nominal` sample index (`write_ensemble_manifest(...; nominal=)`, `read_ensemble_nominal`), `run_monte_carlo_visualization(...; nominal=seed)` runs the unperturbed configuration as one more sample labelled "nominal", and the page payload carries its position.
- src/analysis/visualization/scene/viewer_bundle.jl — textures, base64 float blocks, frame decimation, import map of `data:` URLs, model payloads (Draco/meshopt/KTX2 refused with a fix-it message), `path_payloads`, `reference_payloads` (new: Float64 time/position tables with optional velocity and attitude, target spacecraft, colour, opacity), `model_articulations` (per-id parts to pose, resolved against the raw triangles so the centring uses the posed geometry), the `video.js` module and the `mp4-muxer` vendor entry in the import map, `viewer_payload`, `render_viewer_html`, `export_visualization(...; references=...)`, `write_viewer_dev_payload`. The model-format helpers it used to define moved to `Structure`.

## Findings
- **P2 (found and fixed during review): the selected-spacecraft panel reported a wrong latitude, longitude and altitude (altitude equal to minus the polar radius) on every page.** `globe.rotationAt(t, out)` wrote the quaternion with `out.set(x, y, z, w)`, which is correct for a `THREE.Quaternion` but a no-op for the `Float32Array` that `selectionInfo` passes, so the body-frame position was rotated by a zero quaternion. Planet-fixed playback was unaffected because it passes a `THREE.Quaternion`. Fixed in `viewer/src/globe.js` by branching on `isQuaternion`; verified in headless Chromium that the readout now carries the geodetic values.
- **P2: the "ISS is back to a box" report could not be reproduced.** The ISS orbit page and its CDN variant both load the glTF (`glb (36 meshes, ×2.4)` in the model-status row) and draw the station in follow mode under headless Chromium. Boxes stay visible only when the parse fails or never returns; the status row now names the failure, and the ghost feature reuses the same loader so a failure shows in both places. Left open pending a report of the status row's text from the affected browser.
- **P2 (design, recorded): the depth-buffer shadowing zeroes every facet behind another along the mean flow, including the back facets of a convex body near the limb, which in free-molecular theory still receive the thermal part of the flux.** On a sphere this costs 1.5% of the drag at speed ratio 3 and 0.3% at 8; at aerobraking speed ratios (5 to 10) it is below the accommodation uncertainty. The box case is unaffected because the Hart closed forms drop the back faces too, which is why a box mesh reproduces them to round-off. The unit test pins both numbers.
- **P3: the surrogate clamps the speed ratio to the fitted range silently.** Outside (3, 20) by default the coefficients are those at the boundary; the fit range is stored and documented, but a run at very low speed ratio will not warn.
- **P3: a link without a surrogate carries no aerodynamic load.** Intended for the whole-vehicle case (vestigial wing links), documented, but a per-link fit that forgets a link silently loses its drag.
- **P3: `calcForceTorque` on the legacy (non-sampled) path reads the propagated attitude through `hasproperty(x, :q)` and otherwise falls back to the stored root quaternion**, the same limitation the fM legacy path has; the engine uses the sampled `wrench` path for this effector, so the fallback is not reached in `run_simulation`.
- **P1 (pre-existing, found and fixed during review): GRAM runs sampled the atmosphere at the wrong date.** `GRAMAtmosphereModel(planet_name=...)` defaults its epoch to 2000-01-01, GRAM's ephemeris hook and native date both read that epoch plus the elapsed time, and nothing in the engine or the examples set it to the run's `initial_time` (none of the examples pass one). On Venus at 149 km that put Magellan's periapsis on the night side (5.6e-11 kg/m³, 132 K) where SPICE puts it at 13:24 local solar time (GRAM at the right epoch: 1e-9 kg/m³, 246 K, the value the flown energy loss implies), an order of magnitude in drag; on Mars and Earth the same defect moves local time and season. Fixed by `with_density_model_epoch` in `run_simulation`; a new unit test covers the identity path and, when GRAM is present, the rebuild. Every previously published GRAM-backed number (the telemetry validation studies included) was produced with the model's own epoch unless the manifest set one, and should be checked.
- **P3: articulations select vertices, not triangles.** A triangle with vertices on both sides of a region boundary is stretched. Fine for separate parts such as wings on booms, wrong for a region cutting through a single mesh; documented, not guarded.
- **P3: the MP4 export downloads through a page-started link**, which the claude.ai artifact host blocks; the dialog says so. Verified headlessly on the standalone page: 121 frames at 1280x720, Baseline H.264, a valid fast-start MP4 (`ftyp`, `moov`, `mdat`).
- **P3: `render_viewer_html` escapes `<`, `>` before `&`**, so a title containing `<` becomes `&amp;lt;`. Cosmetic (titles are set by the caller); should be reordered in a follow-up.
- **P3: `_gltf_accessor` reads buffer slices under `@inbounds`**, so a truncated or malformed GLB with a bad `byteOffset` or `count` can read out of bounds instead of raising. Only reachable through a caller-supplied model file; the STL reader validates lengths first.
- **P3: `_density_sample` swallows every exception** (bare `catch`) and returns `nothing`, so a broken density model yields an empty profile with no diagnostic. Only reached with the flag on.
- **P3: `_save_density` reads `shared_buffers.densities`**, written by the density callbacks and the flat-RHS prefill; a route that samples with `write_buffers=false` would leave a stale column. The three mission drivers (serial, GRAM) show the expected profiles; a non-flat multi-satellite run with the flag on is untested.
- Design notes recorded rather than flagged: `with_visualization_scene` and `with_results_directory` rebuild `SimulationSettings`/`SimulationConfiguration` field by field (a future field is silently dropped on the visualization path, the same pattern `_ensemble_member_settings` already used); `export_visualization` runs inside `run_simulation` when `visualization=true`, so a missing `viewer/` tree fails the run after the results are on disk; the CDN page builder concatenates the viewer modules into one scope, so top-level names must stay unique across modules (checked with `node --check` on the concatenation).

## P1 Assessment
One P1, pre-existing (GRAM epoch), found and fixed in this PR; unresolved P1: No. Otherwise every runtime hook is gated: the settings flag defaults to false, the `run_simulation` keyword defaults from the env adapter, `visualization_save_fields` is empty with the flag off, and the sidecar and page hooks short-circuit. The only unconditional changes on the default path are one `using` line, an empty append in `default_save_fields`, and the module include at load time; no numerical or output-schema change without the flag. Unresolved P1: No

## Tests Added/Updated
- test/unit/visualization/scene_visualization_tests.jl (new): sidecar build and JSON round trip, schema check, link-pose layout, `with_visualization_scene`.
- test/unit/visualization/viewer_bundle_tests.jl (new, extended in this round): texture manifest and tiers, frame decimation and Float64 positions, model formats and the Draco refusal, geometry readers and point clouds, reference paths, `reference_payloads` (encoding, defaults, every validation branch, the import-map entry for `references.js`), STL overrides, import map and HTML assembly, end-to-end export from a flagged run including the CLI and a page with a ghost.
- test/unit/visualization/articulation_tests.jl, arm_tests.jl, ensemble_tests.jl, atmosphere_tests.jl (new): link/arm pose fields against the integrated state, ensemble manifest and resampling, atmosphere sampling (GRAM verified separately).
- test/unit/runtests.jl registers the visualization directory; test/suites/02_callbacks_parallel_and_smoke_tests.jl covers the save-field gate; test/gates/ci_path_policy_gate.jl records `src/viewer` as retired (the viewer is top-level).
- test/unit/dynamics/mesh_aero_tests.jl (new, registered in `test/unit/runtests.jl`): panel construction and defaults; a UV sphere against the closed-form free-molecular drag (0.5% without shadowing, 2%/0.6% with it), the `sqrt(Tw/T)` split against the diffuse term of that formula, and the projected area; a box against `aerodynamic_coefficient_fM` at two speed ratios and four (alpha, beta) pairs to 1e-9 in drag and 1e-8 in the lateral magnitude; a plate behind a plate (shadowed, unshadowed, offset); orthonormality of the harmonic basis on a Fibonacci quadrature; the surrogate fit, holdout error, clamping, JSON round trip and constructor validation; and a 300 s cube run under the surrogate against the same cube under the fM model (specific-energy loss within 10%, drag save field populated).
- test/unit/dynamics/mesh_aero_tests.jl gains an articulation testset (a plate rotated about its centre, the payload's axis/pivot/region, the posed geometry seen by the panel method, and the Magellan wing turning thin along the flow axis); test/unit/visualization/viewer_bundle_tests.jl checks `model_articulations` in the page payload and the video module and muxer in the import map.
- test/unit/simulation/density_epoch_tests.jl (new, registered): identity for models without an epoch, `run_simulation`'s alignment leaves such a configuration untouched, and with GRAM available the rebuild at the run epoch, its idempotence and the explicit-epoch construction. test/unit/visualization/ensemble_tests.jl (51) passes with the manifest's new `nominal` key.
- Viewer JavaScript has no unit harness; every module and the CDN concatenation pass `node --check`, and the pages were rendered headlessly with model status, ghost status and separation read back through `window.spaceagoraViewer`.

## Residual Risk
Moderate, concentrated in the viewer rather than the runtime. The Julia side is additive and gated, and the unit tests cover the data contract end to end, so the default simulation path carries no new risk. The browser side is verified only in headless Chromium here and in the user's Firefox earlier; a rendering difference in another browser (the box report) has no automated catch beyond the status row. The mission drivers depend on external assets (NAIF kernels downloaded on first run, Venus/Titan GRAM) and are dev tooling, not CI. The reference ghosts assume the run's inertial frame is J2000 centred on the primary, which holds for the SPICE ephemerides path used by the drivers; a run on the simple ephemerides would need its own sampling. The standalone page reproduces the bundler's payload in JavaScript (IAU rotation instead of SPICE; leap seconds ignored), verified headlessly with the Apollo CSV and a five-file ensemble but not covered by the Julia tests. The mesh aerodynamics path is validated against closed forms and the existing box model, not against flight data; its accuracy on a real vehicle rests on the accommodation coefficients, the wall temperature and the mesh's winding, and the depth-buffer shadowing is an approximation of the directional flux (see the P2 note). The Magellan and Cassini drivers can run either model against the SPICE ghost (`SPACEAGORA_DEMO_MESH_AERO=1`), which is the intended check; both cases now fly with the long axis along the flow, and for Magellan that puts the NASA model's wings edge-on (the cruise geometry) while the box model keeps them broadside, so the two aerodynamic models differ by design there until an articulated wing mesh exists. With the GRAM epoch fixed, two aerobraking orbits of Magellan end 2.4 km from the navigation kernel with the box model (energy loss 13% above the kernel's) and 0.7 km with the mesh surrogate flown bus-and-antenna forward (9% below; 1.0 km antenna aft), the two aerodynamic models bracketing the flown trajectory; Cassini stays within 5 km over five hours with drag negligible. These separations are reported on the pages rather than hidden.
