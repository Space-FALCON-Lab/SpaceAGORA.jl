# AI Review Artifact for PR #127: Configuration copies and shared mathematics

Reviewed commits: `c0f1b5da` and `9b5d1646`. Review baseline: `fdbc6229` from #126. Publication combines these unchanged commits with main `15d14654` at `c51eeb5a`, after #123, #124 and #126 merged.

## Scope

Migrate 29 existing-configuration copies and replace repeated frame/quaternion inclusion with single internal owners. Preserve deliberate overrides, telemetry retry precedence, root exports and runtime isolation. Account explicitly for module-dependent floating-point rounding and the repaired missing ephemerides bindings. This review does not claim a performance improvement.

## Changed Files

- `src/analysis/verification/telemetry_verification.jl`: import the private configuration helper and canonical frame owner.
- `src/analysis/verification/telemetry_verification/runner.jl`: use the helper while explicitly clearing the typed solver for telemetry selection and retries.
- `src/analysis/verification/telemetry_verification/scenario_builders.jl`: replace outer configuration copies, retaining nested overrides.
- `src/core/numerics/quaternion_utils.jl`: wrap unchanged function bodies in the private QuaternionMath module.
- `src/core/simulation_model.jl`: load QuaternionMath once and declare FrameTransforms with explicit ephemerides imports.
- `src/dynamics/coupled/force_torque_models/aerodynamic_effectors.jl`: import the canonical frame functions, repairing the missing ephemerides-binding path.
- `src/dynamics/coupled/force_torque_models/perturbation_effectors.jl`: import geodetic conversion directly from its owner.
- `src/dynamics/coupled/perturbations.jl`: import shared quaternion functions.
- `src/gnc/control/control_hooks.jl`: import shared quaternion functions and explicit dependencies.
- `src/gnc/control/propulsive_maneuvers.jl`: import shared frame functions; consumer rounding may differ from its former compiled copy.
- `src/gnc/guidance/guidance_hooks.jl`: import shared frame functions and dependencies, repairing the missing ephemerides-binding path.
- `src/simulation/callbacks/registry.jl`: import shared frames and make StaticArrays dependency explicit.
- `src/simulation/campaigns/constellation_ensemble.jl`: use field-preserving configuration updates while retaining member overrides.
- `src/simulation/engine/setup.jl`: use the canonical frame owner.
- `src/vehicle/kinematics/kinematics.jl`: use the canonical quaternion owner.

- `docs/architecture/canonical_topology_contract.md`: document configuration-copy and shared-math ownership and conventions.
- Nine examples and five benchmark/study files: replace outer configuration rebuilds with locally qualified helper calls, preserving nested expressions.
- `test/probes/configuration_copy_probes.jl`: preservation, intentional resets, campaign members and actual telemetry retry checks.
- `test/probes/shared_math_ownership_probes.jl`: function identity, conventions and frame-context checks.
- `test/probes/reference_system_probes.jl`: exercise the canonical production owner while retaining the intentional raw error-path sandbox.
- Existing configuration-copy consumers in test probes, smoke and suites 02/04: helper migration. Suite 09 registers the ownership probe.

The private field-preserving helper and smoke-builder migration from #126 are already merged in the publication base and are reviewed in that PR.

## Findings

The configuration commit is accepted without code corrections. The shared owner is structurally correct; the blocking review finding was an overbroad equivalence claim and incomplete retention of the affected example comparison. Publication text now states numerical agreement within floating-point rounding for the reviewed comparisons, rather than universal cross-version bit identity. The retained example comparison applies an explicit diagnostic tolerance while separately requiring identical saved trajectory columns. Engine-binding sample equality does not establish equality for every former compiled copy.

The shared owner's explicit ephemerides imports repair undefined references in the guidance/aerodynamics five-argument frame conversion. The flat/serial bit-identity contract within one tree is unchanged; cross-version comparison is a different contract. This does not assert that every possible parallel workload was rerun.

The original bundle's configuration runs predated the final commit and most shared-math children preceded its declaration-placement amendment. Those logs lack per-run source SHAs. Claude revalidated final head `9b5d1646`; that review, not a retrospective attribution of old logs, supplies final-head evidence.

## P1 Assessment

No unresolved implementation P1 finding was identified in the bounded independent review. The publication-blocking wording/evidence correction is addressed separately from source changes. Final CI remains required.

## Tests Added/Updated

On combined head `c51eeb5a`, configuration preservation (338), initial altitudes (268), shared ownership/conventions (367), reference-system probes (665), thruster guidance (96), the real guidance sandbox testset (9), and all 23 PR contract gates pass with four Julia threads. Each retained log records its source SHA, command and exit code.

338 configuration and 367 ownership assertions pass in the independent final-head review. Reference and thruster probes, 23 PR and 27 local gates, migrated examples, threaded smoke/probes, two light benchmark scripts and the Odyssey quick comparison pass in the documented scopes. A separate retained before/after energy-depletion comparison records source hashes, diagnostic tolerances and saved-state equality. The original two-thread failure and passing four-thread rerun are retained. No historical runner self-test is described as proof of its untested child-loop aggregation path.

## Residual Risk

Cross-version rounding-level changes are expected for former module-local consumers; exact trajectory identity is limited to the executed comparisons. Full integration, documentation build, Linux CI for this follow-up, runtime with other open PRs, performance-harness measurements and the Monte Carlo study remain pending. The installed GRAM rig is not proof of a reproducible public installation. #121 adds more configuration copies and a settings field requiring separate migration. Merge checks use actual merge bases and establish textual compatibility only. No public telemetry error values are included in this artifact.
