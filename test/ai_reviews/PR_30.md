# AI Review — PR #30

## Scope
- Repairs the `julia-ci.yml` workflow, which GitHub's parser has rejected on every push since the `clean-depot-smoke` job was added in 837a1b2a (job-level `env` used the `runner` context, which is only available inside steps), so none of its 13 jobs ever ran.
- Hardens the revived workflow from its first real runs: disk-space cleanup before each ~3 GB dev-GRAMSuite submodule checkout (threaded-smoke hit ENOSPC), root-project `Pkg.instantiate` in the calibration-coverage and contract-suite jobs (both failed on missing deps on cache-miss runners), and the `push` trigger scoped to main/master so PR branches do not run everything twice.
- Makes `ci_ai_review_artifact_gate` skippable for PRs that change no `src/**/*.jl` files, since the artifact exists to cover src changes and its filename depends on a PR number that cannot exist before the PR is opened.
- Reviewed: workflow YAML against `actionlint` (GitHub's context-availability rules), gate logic against both the artifact-present and no-src-change paths.

## Changed Files
- None — this PR changes no `src/**/*.jl` files. It touches `.github/workflows/julia-ci.yml`, `test/ci_ai_review_artifact_gate.jl`, and adds this artifact.

## Findings
- No findings.

## P1 Assessment
- The workflow-file rejection silently disabled 13 CI jobs (including coverage and P1 gates) for months; this PR resolves it.
- Unresolved P1: No

## Tests Added/Updated
- None — CI configuration and gate-policy change only. The CI run itself validates that the workflow parses and that all revived jobs execute; the gate's skip path is exercised by this very PR (no src changes, artifact optional) and the artifact-validation path by this file.

## Residual Risk
- The coverage-quality gates will produce their first real per-file numbers on this PR's runs; files that drifted below thresholds while the workflow was dark may need targeted tests or documented overrides in a follow-up.
- The `sudo rm -rf` disk-cleanup step removes preinstalled toolchains (dotnet, Android, ghc, CodeQL) from ephemeral runners only; if a future job needs one of these, drop the corresponding path from that job's cleanup step.
