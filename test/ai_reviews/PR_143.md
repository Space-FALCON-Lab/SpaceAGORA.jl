# Gravity-gradient integration review

## Scope

Refresh the standalone central-field gravity-gradient torque effector from Evan's PR121, including its reviewed thread-safety correction from `bd5899acbf5670cbff440aff3b49f3e6ead80c3c` onto actual main `29e6d92d216012779a40a9c1b2a2f940f3b2d59e`. The automatic merge has no conflict. The aggregate force/torque module retains main's mesh and reaction-wheel exports plus the gravity effector. Engine setup retains both the mesh additions and the gravity thread-safety specialization. The unit runner preserves all 45 registrations. The existing shared CI canary correction from viewer head `af8f3abe` is copied exactly; no runtime calibration or routing behavior changes.

## Changed Files

- `src/environment/gravity/gravity_models.jl`: adds the opt-in effector and delegates both supported interfaces to the existing torque owner.
- `src/dynamics/coupled/force_torque_models/gravity_effectors.jl`: owner export.
- `src/dynamics/coupled/force_torque_models.jl`: aggregate import/export, preserving main's reaction-wheel integration.
- `src/environment/gravity/gravity_effectors.jl`: environment namespace forwarding.
- `src/simulation/engine/setup.jl`: declares the stateless torque effector thread-safe so adding it does not disable the flat route.
- `test/unit/environment/gravity_gradient_torque_tests.jl`: independent vector-law, real RHS, propagation, concurrent reuse and serial/flat parity regressions.
- `test/unit/runtests.jl`: registers the gravity test once while preserving every current-main test include.
- `docs/src/user/custom_effector.md`: usage, assumptions and double-counting caveat, preserving the reaction-wheel documentation.
- `test/ai_reviews/PR_143.md`: review scope, integration evidence and validation limits.
- `test/unit/parallel/cost_machine_calibration_tests.jl`: exact already-reviewed timing-test correction from PR148; production timing code and thresholds unchanged.

## Findings

Both supported interfaces reuse the canonical central-field torque owner and add exactly zero translational force. Selection uses run indices. The stateless effector remains explicitly thread-safe, and the regression exercises actual serial, forced-flat and automatic-route propagation with harmonics. All feature/runtime changes are the preserved reviewed implementation; this refresh changes no physical formula.

Independent source preservation review found no integration blocker. The canary test checks each real sample's finite positive measurement, signed relative drift and the unchanged inclusive 25% decision. It does not assume successive timings agree; there are no retries, skips or widened production tolerances. The pending viewer integration merges cleanly at the reviewed heads. Static merge compatibility is distinct from combined runtime validation.

## P1 Assessment

No source or integration P1 finding in the reviewed refresh. Fresh current-head Linux CI remains required before merge.

Unresolved P1: No

## Tests Added/Updated

Executed on `164b129d7a000988000b52fe18301af15a30156d`, tree `8fb5637d5f8005d589acded43ddbba7f12192fdb`, Julia 1.12.3 with two threads: 229 gravity gradient torque, 1725 mesh aero, 147 plume surface interaction, 60 cost machine calibration; 2161 focused assertions total. All 23 contract gates, the numbered artifact gate and strict documentation including external links pass. Both runners exit zero and consumed source hashes remain unchanged.

The final follow-up updates only this review record. The artifact gate is rerun on that final commit; runtime, tests and user documentation match the executed candidate exactly. No native GRAM was requested. Earlier evidence separately passed terrain and reaction-wheel coexistence regressions. The retained gravity tests compare saved times, positions, attitudes and angular rates bit for bit for their bounded fixture; this is not an arbitrary-mission parity or performance claim.

## Residual Risk

The torque is a central-field approximation. Enabling it alongside an existing gravity-gradient option double-counts torque, as documented. The threaded route regression needs at least two threads; this refresh uses two. Local coexistence tests do not establish every combined-mission interaction. Full unit/integration/nightly suites, native-SPICE landing, physical calibration and performance measurements were not rerun locally. These local results are not a substitute for the published head's CI. Main or PR-head changes require checking the new combination before merge. The viewer/terrain publication sequence remains first; this refresh does not authorize bypassing that sequence or its checks.
