=== ENTIRELY LASER/ORACLE-RELATED (NEW FILES) ===

src/dynamics/coupled/force_torque_models/laser_link_effectors.jl
  → The entire new laser force module

src/dynamics/coupled/force_torque_models/laser_link_effectors_ver1.jl
  → Prior version of the laser module

src/dynamics/coupled/force_torque_models/laser_link_effectors copy.jl
  → Scratch copy

scripts/plotting/oracle_case2_laser_grid_plots.jl
  → ORACLE Case 2 laser-link plot script


=== PARTIALLY MODIFIED — LASER ADDITIONS ARE THE MAIN CHANGE ===

src/SpaceAGORA.jl
  → Swapped out run_constellation_ensemble imports/exports for
    OpenCavityLaserLinkModel, LaserImpulseTracker, laser_link_scheduler_callback,
    laser_impulse_callback, tracked_dv_at

src/dynamics/coupled/force_torque_models.jl
  → Added include for laser_link_effectors.jl and all its using/export lines;
    removed robot_arm_reaction_effector

src/simulation/engine/dynamics_rhs.jl
  → Added entire _constellation_coupled_effector /
    _accumulate_constellation_coupled_effectors! dispatch path specifically for
    OpenCavityLaserLinkModel — this is the integration point for laser forces in the RHS

test/suites/04_solver_env_and_regression_tests.jl
  → Added @testset "Open Cavity Laser Link Effector" unit test

test/suites/01_contract_and_api_tests.jl
  → Added contract assertions that laser_link_effectors.jl exists, exports the
    right symbols, and is wired into force_torque_models.jl

docs/public_api_symbols.jl
  → Added OpenCavityLaserLinkModel and laser_link_scheduler_callback to the
    public API surface list


=== NEW TESTS — PARTIALLY LASER-RELATED ===

test/ci_scaffold_interface_gate.jl
  → Tests LaserTerminal actuator scaffold from experimental/vehicle/laser_terminal/;
    enforces that apply_laser_terminal_command! throws "Not implemented: ..." rather
    than silently doing nothing

test/ci_src_completeness_contract_gate.jl
  → Checks that src/vehicle/actuators/laser_terminal does NOT exist in src/,
    preventing premature promotion of the laser terminal from experimental/ to src/
