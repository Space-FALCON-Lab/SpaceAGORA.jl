# AI Review — PR 67: relocate the CYGNSS attitude study to the private telemetry repo

## Scope
PI direction on repository separation: the public SpaceAGORA.jl repo carries replication *capabilities* — dynamics, field models, effectors, fixes like the #64 dipole sign correction — while studies over flight telemetry, their drivers, and their results live in the private telemetry repo, with results published to the campaign results log. `benchmarks/studies/cygnss_attitude_torque_reconstruction/` was a telemetry study (drivers + results tables over CYGNSS flight data) and is relocated accordingly.

## Changed Files
- `benchmarks/studies/cygnss_attitude_torque_reconstruction/` (removed, 8 files) — mirrored in full to the private telemetry repo before removal, including the post-#64 rerun of its results table and regenerated plot data.

## Findings
- No live dependencies: the only public-repo references to the study path are historical review artifacts (PR_49.md, PR_64.md), which remain as audit records.
- All capabilities the study exercised remain public: `MagneticTorqueRodModel`/`get_magnetic_field_dipole` (sign-fixed in #64, IGRF option added), per-link atmosphere sampling, free-molecular aero, wheel/GG effectors. Nothing in `src/` referenced the study.
- `benchmarks/studies/cygnss_estimator_validation.jl` is left in place (separate file, no telemetry results, known-broken on main pending the perturbation-mc branch) — flagged to the PI rather than bundled here.

## P1 Assessment
No P1 findings. Deletion only; no src changes; CI does not reference the removed paths.
Unresolved P1: No

## Tests Added/Updated
- None; no behavior to pin. The full CI matrix on this PR is the no-dependency proof.

## Residual Risk
- Anyone following an old external link to the study path will find it gone; the git history retains it, and the private mirror is the living copy.
