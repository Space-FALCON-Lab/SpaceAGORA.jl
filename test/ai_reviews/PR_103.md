# AI Review Artifact for PR #103

## Scope
Deterministic (collect-then-sum) reductions for the threaded force loops: effector loop (both variants), n-body inner loop, multibody aero loop. The serial path is bit-for-bit unchanged; the threaded paths now reproduce it exactly for any worker count. No physics change.

## Changed Files
- src/parallel/policy/thread_execution.jl — `threaded_collect!`, `threaded_collect_persistent!` (plus do-block orderings): evaluate into per-item slots, no accumulation on workers.
- src/simulation/engine/dynamics_rhs.jl — `_accumulate_dynamic_effectors!` and the partitioned variant use the collect helper and sum in effector order with the serial statements; the threaded path now accumulates into `forces`/`torques` like the serial path instead of assigning.
- src/core/types/runtime_types.jl — `NBodyScratchWorkspace` holds `body_force_ii` (per body); `AeroScratchWorkspace` holds per-link slots.
- src/dynamics/coupled/perturbations.jl — `_nbody_body_force_ii` shared by both paths; per-body collect then body-order sum; workspace maker/capacity updated.
- src/dynamics/coupled/aerodynamic_wrench_models.jl — per-link collect then link-order sum; workspace maker/capacity sized by links.

## Findings
- P2: the old threaded effector path assigned the reduced total to `forces`/`torques` while the serial path accumulated. The new threaded path accumulates, matching serial. Verified bit-identical on Odyssey and on the constellation smoke; if any caller had relied on assignment semantics with a non-zero input, the smoke comparison would differ. Resolved.
- P2: the n-body serial expression was restructured into a shared function (same `@fastmath` expression). Verified: the serial Odyssey fingerprint equals `main`'s. Resolved.
- P3: aero workspace capacity is now per link instead of per thread; `_aero_workspace_for_sat!` keeps its parameter name (value is now the link count). Cosmetic; noted.
- P3: the multi-satellite flat effector queue still accumulates per worker. Out of scope here (PR #90 territory); recorded in the PR description.

## P1 Assessment
No P1 findings. Unresolved P1: No

## Tests Added/Updated
- test/smoke/ci_threaded_smoke.jl — thread-count independence check (serial vs 2/4 effector workers, n-body on 2 workers, all on) on a 3-effector, 2-satellite constellation; bit-identical required.
- test/probes/parallel_policy_probes.jl — collect helper unit checks.
- Manual: Odyssey quick fingerprint sweep (6 configurations, one fingerprint, equal to main's serial); contract gates 45 pass.

## Residual Risk
Low. Per-call allocation of a small contributions vector in the threaded effector path (n_effectors entries); negligible next to an effector evaluation. Cross-host results still differ at the last bit (different CPUs/libm), which is expected and separate from thread-count independence.
