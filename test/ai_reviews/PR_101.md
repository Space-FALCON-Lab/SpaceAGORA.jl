# AI Review Artifact for PR #101

## Scope
Telemetry verification runner: add a scenario selection (`--scenarios=`, `SPACEAGORA_TELEMETRY_SCENARIOS`, CLI passthrough) and reuse the calibration eval solve as the final solve when the candidate grid has a single point and the profiles match. No physics or grading change; the grid-search path is untouched.

## Changed Files
- src/analysis/verification/telemetry_verification/types.jl — `scenarios` field on `StudyConfig` and `VerificationRequest`; `_parse_scenario_list`.
- src/analysis/verification/telemetry_verification/manifest_parsing.jl — `--scenarios=` in `parse_cli`; request/config round-trip carries the selection.
- src/analysis/verification/telemetry_verification/calibration.jl — `_single_point_calibration` predicate.
- src/analysis/verification/telemetry_verification/runner.jl — `_select_scenarios`, `_final_run_or_reused_eval`; both scenario kinds keep the eval run and reuse it when the grid is a single point; CLI request carries the selection; log line for the selection.
- src/cli/spaceagora_cli.jl — `--scenarios=` passthrough to the launcher via the environment.

## Findings
- P2: the reuse must not change grading. Checked: the final args are built from the same single candidate the eval used (same cd_scale, cr, orbit count, profile), so the configurations are identical; the bias fit is applied to the final rows as before. Resolved by construction and covered by the unit checks.
- P3: an unknown scenario name must not silently run nothing. `_select_scenarios` raises an `ArgumentError` listing the manifest names. Resolved.
- P3: the CLI passthrough sets `SPACEAGORA_TELEMETRY_SCENARIOS` to an empty string when the flag is absent, which the parser treats as "all". Verified with `--print-only`.

## P1 Assessment
No P1 findings. Unresolved P1: No

## Tests Added/Updated
- test/probes/coverage_parallel_telemetry_probes.jl — CLI selection parsing, env fallback, request round-trip, `_select_scenarios` (incl. unknown-name error), `_single_point_calibration` truth table, `_final_run_or_reused_eval` reuse and solve paths.
- test/suites/06_monolith_split_runtime_tests.jl — selection survives the request/config round-trip.
- Manual: quick profile with `--scenarios=odyssey`; full profile Odyssey locally (effector threading off) to observe the single solve; contract gates (45) pass.

## Residual Risk
Low. A scenario whose calibration grid has one point now runs one solve instead of two identical ones; a scenario with a real grid is unchanged. If a future manifest sets `search_on_quick_subset` with a single-point grid, the profiles differ and the runner solves twice as before.
