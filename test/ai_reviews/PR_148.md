# PR 148: viewer package integration review

## Scope

Integrate saved scalar channels, shared display-model resources, six optional NASA mission display models, three opt-in higher-resolution textures and four analytic viewer demonstrations from PR #121. Base: main `7e6fdb01` after PR139. The earlier viewer implementation and its browser acceptance are already merged through PR #137.

## Changed Files

- `src/analysis/visualization/scene/viewer_bundle.jl`: validated saved-column channels and embedded model sharing.
- `src/analysis/visualization/scene/ensemble_export.jl`: explicit texture tier selection while preserving 4k defaults.

Also changes viewer data/loading/inspection/reference JavaScript, asset provenance, user documentation, demo launchers, focused tests and a CI step that executes the shipped JavaScript suite. No dynamics or atmosphere implementation is added.

## Findings

The frozen 30-file inventory was checked by SHA256. Current-main integration preserves all existing test registrations, including density workers, worker load paths, wheel momentum and terrain. Two source owners and all four changed JavaScript files remain byte-identical to the previously validated candidate.

Independent review found no concrete source blocker. It identified two integration improvements, implemented here: the 11-case JavaScript suite now runs in the existing README smoke job before runner cleanup, without npm; the user guide includes a saved-mass channel example, plotting, absent columns, missing-data gaps and decimation. Model loaders share geometry for ordinary clones while isolating transforms/materials; articulations and skinned models retain private geometry. Scalar metadata passes the escaped text boundary. Channel samples use Float32 and the trajectory's retained rows.

## P1 Assessment

No concrete source P1 finding remains in the bounded static review. Browser acceptance of the new features is a merge requirement and has not been completed by Codex.

Unresolved P1: No

## Tests Added/Updated

On the exact integrated candidate, Julia 1.12.3 with two threads: 670 focused Julia assertions and all 23 contract gates passed. The actual-source JavaScript suite passed all 11 cases. Strict documentation passed with external link checks enabled and unchanged size thresholds; the public API page is 166.83 KiB, below the 200 KiB hard limit. Both standalone builders passed, preserving 4k defaults and explicit 8k Earth/Mars/Moon tiers; every embedded changed renderer module matches its source. All 31 candidate files were unchanged during validation and must hash-match publication. Full Linux CI and full unit/integration suites remain pending. The four complete demo runs are retained from the earlier frozen base and are not relabeled as current-main runs.

## Residual Risk

Browser rendering, readability, shared-model visual independence, asset appearance and texture quality require independent visual acceptance. This session's browser policy blocks local-page access; no workaround was attempted. Physics, mission calibration, terrain export and the complete real-DEM landing demonstration are outside this package. The meshes are display assets, not engineering geometry. The assets add about 35.6 MB to checkouts. Full Linux CI remains pending on the eventual PR head. No measured speedup is claimed.
