# PR 148: viewer package integration review

## Scope

Add saved scalar channels, shared display-model resources, six optional NASA mission display models, three opt-in higher-resolution textures and four analytic viewer demonstrations from PR #121. This integration merges main `8eaef4d6` into reviewed viewer head `503da078`. The sole conflict is an additive unit-test registration: both the viewer demo-options tests and the newly merged mesh-aerodynamics tests are retained, as is main's density-epoch registration. No atmosphere or dynamics implementation is changed by the viewer package.

## Changed Files

- `src/analysis/visualization/scene/viewer_bundle.jl`: validated saved-column channels and embedded model sharing.
- `src/analysis/visualization/scene/ensemble_export.jl`: explicit texture-tier selection while preserving 4k defaults.

Also changes viewer JavaScript, asset provenance, user documentation, demo launchers, focused tests and the JavaScript CI step. All reviewed viewer runtime, JavaScript, assets, tests and user guides remain byte-identical to `503da078`; the integration resolution changes only the combined unit-test registration and this review record beyond the automatic merge.

## Findings

Shared models preserve per-instance transforms and materials; articulated and skinned models retain private geometry. Scalar metadata passes the escaped-text boundary. Channel samples use Float32 and the trajectory's retained rows. The actual-source JavaScript suite runs in the README smoke job without npm.

The corrected guide explains output paths, results prefixes, spacecraft selection, the selection panel, saved spherical-periapsis-altitude plotting, silent omission of absent columns, log-axis zero gaps, held-state flatlines and space-separated demo options. The source-based readability blockers are resolved.

## P1 Assessment

No blocking source or browser finding remains. Independent integration preservation review passed. Fresh current-head CI is required for the updated merge head.

Unresolved P1: No

## Tests Added/Updated

Historical implementation validation at `6665855e`: 670 focused Julia assertions, 23 contract gates, 11 actual-source JavaScript cases, strict documentation with external link checks, and both 4k/8k standalone builds with embedded-source equality. The readability follow-up at `503da078` passed strict documentation and its focused gates. All 13 GitHub checks on `503da078` subsequently passed; these results do not establish CI success for the refreshed integration head.

The refreshed integration passes 670 viewer assertions, 1725 mesh assertions and 122 atmosphere-epoch assertions, all 23 contract gates, the numbered artifact gate, all 11 JavaScript cases, both standalone builds with embedded modules matching source, and strict documentation with external link checks. All source hashes remained unchanged during execution. The final artifact wording corrects the fixture origin and records these results; its gate is rerun on the final commit. Fresh Linux CI remains pending until the updated head completes.

## Browser acceptance

Claude independently accepted unmodified fixtures generated at `6665855e`, still applicable to `503da078` after its documentation-only correction, retained at private commit `b27e70e`. All five browser checks passed with six fixture hashes verified: custom values and gaps/plots, shared-model instance independence, six display assets, three texture choices and saved arm poses. No console errors or external network requests were observed. The page was sampled at fixed timeline positions because the browser was throttled; this does not measure continuous-playback performance. Renderer files are unchanged in this integration, so that visual acceptance remains applicable. Codex's own local-page route was denied by browser policy and was not bypassed.

## Residual Risk

The four complete demonstration runs are retained from an earlier frozen base, not relabeled as current-main runs. The later 60-second arm simulation produced its expected rows and changing poses, but its generator wrapper failed during bookkeeping; the output was independently checked. Full Linux CI and full unit/integration suites are pending for the refreshed head. Physics calibration, terrain export, full real-DEM landing and performance measurements are separate work. Display assets are not engineering geometry and add about 35.6 MB to checkouts. Nonblocking browser observations remain documented: labels/rings are not selection targets, Follow may need toggling after selecting another craft, and sparse ghost references can differ from a curved trajectory between samples.
