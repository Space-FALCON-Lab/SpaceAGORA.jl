# PR 148: viewer package integration review

## Scope

Add saved scalar channels, shared display-model resources, six optional NASA mission display models, three opt-in higher-resolution textures and four analytic viewer demonstrations from PR #121. The latest integration combines viewer `2e4f3d4f` with main `29e6d92d`, preserving both sides of the additive unit-test registration conflict. All 45 registrations occur once. Viewer, plume and other production code retain their parent identities.

A test-only correction checks performance-calibration freshness against each actual timing sample. The prior test required two separate measurements to stay within 25%; CI observed about 29% drift, which production correctly rejected. Host contention is a plausible explanation, not an established cause. The production threshold, timing implementation and routing behavior are unchanged.

## Changed Files

- `src/analysis/visualization/scene/viewer_bundle.jl`: validated saved-column channels and embedded model sharing.
- `src/analysis/visualization/scene/ensemble_export.jl`: explicit texture-tier selection while preserving 4k defaults.

Also changes viewer JavaScript, asset provenance, user documentation, demo launchers, focused tests and the JavaScript CI step. The final integration changes only the unit registry, calibration test and this review record beyond the automatic merge; the reviewed viewer implementation is unchanged.

## Findings

Shared models preserve per-instance transforms and materials; articulated and skinned models retain private geometry. Scalar metadata passes the escaped-text boundary. Channel samples use Float32 and the trajectory's retained rows. The actual-source JavaScript suite runs in the README smoke job without npm.

The guide explains output paths, results prefixes, spacecraft selection, the selection panel, saved spherical-periapsis-altitude plotting, silent omission of absent columns, log-axis zero gaps, held-state flatlines and space-separated demo options. The source-based readability blockers are resolved.

Independent integration review verifies all viewer-only and main-only file identities, the exact registry union, and unchanged production calibration/timing code. The revised canary checks finite positive measurements, signed dimensionless drift, the inclusive unchanged 25% decision and invalid references. No retries, skips or wider production tolerance are introduced. Live sampling does not guarantee exact threshold-boundary coverage.

## P1 Assessment

No blocking source or browser finding remains. Fresh current-head CI is required after the repair.

Unresolved P1: No

## Tests Added/Updated

Executed on `594b41cc10c16e23f4aab3e126da73748385d66b` (tree `fa6d54eaf036286e7865f1fa98ca492f60704f13`), Julia 1.12.3, two threads: 670 viewer assertions, 147 plume assertions, the full calibration file (60 assertions, including the revised 28-assertion canary), all 23 contract gates and the numbered artifact gate. All 11 JavaScript cases, both 4k/8k standalone builds with embedded-source equality, and strict documentation including external links pass. Source hashes stayed unchanged. The final follow-up only updates this record; the numbered artifact gate is rerun on that final commit.

Earlier integration execution separately passed 1,725 mesh and 122 atmosphere-epoch assertions. Their inherited source remains unchanged; those counts are historical, not additional tests from this repair. Previous green CI and the old timing-test failure do not establish CI success for the new head.

## Browser acceptance

Claude independently accepted fixtures generated at `6665855e`, applicable to readability head `503da078`, retained at private commit `b27e70e`. All five checks passed with six fixture hashes verified: custom values and gaps/plots, shared-model instance independence, six display assets, three texture choices and saved arm poses. No console errors or external requests were observed. The page was sampled at fixed timeline positions because the browser was throttled; this does not measure continuous-playback performance. Renderer files are unchanged in the latest integration, so that acceptance remains applicable. Codex's own local-page route was denied by browser policy and was not bypassed.

## Residual Risk

Full Linux CI and the full unit/integration suites remain pending for the repaired head. The four complete demonstrations are retained from an earlier frozen base. A later 60-second arm simulation produced expected rows and changing poses but its wrapper failed during bookkeeping; the output was independently checked. Terrain export, full real-DEM landing, physical calibration and performance measurements remain separate work. Display assets add about 35.6 MB and are not engineering geometry. Nonblocking browser observations remain documented: labels/rings are not selection targets, Follow may need toggling after selecting another craft, and sparse ghost references can differ from curved trajectories between samples.
