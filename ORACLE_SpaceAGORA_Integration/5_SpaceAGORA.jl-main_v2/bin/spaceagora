#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
PROJECT_DIR="${JULIA_PROJECT:-${REPO_ROOT}}"

exec julia --project="${PROJECT_DIR}" "${REPO_ROOT}/src/cli/main.jl" "$@"
