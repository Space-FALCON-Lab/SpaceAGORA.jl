Home for function-level unit tests migrated out of `test/suites/04_solver_env_and_regression_tests.jl` (solver/env helper parsing, solver-policy internals), mirroring `src/simulation/engine/`.

See `test/TEST_RESTRUCTURE_PLAN.md` for the migration order and `test/README.md` for the convention new tests here should follow.
