# AI Review — PR 68: drop the cygnss_estimator_validation.jl dead stub

## Scope
PI-directed follow-up to the #67 study relocation: `benchmarks/studies/cygnss_estimator_validation.jl` was an 8-line entry point calling `run_cygnss_estimator_validation_cli`, a function that exists only on the unmerged `perturbation-mc` branch — running the script on main fails immediately with an UndefVarError. Under the repository-separation rule (public repo = replication capabilities; telemetry studies = private repo), the driver belongs private when its implementation lands.

## Changed Files
- `benchmarks/studies/cygnss_estimator_validation.jl` (removed) — dead stub, no implementation, no results, no data.
- `docs/src/user/studies_benchmarks.md` — the stub's row entry in the studies table dropped (found by the reference grep; the initial "no references" claim was wrong and is corrected here).

## Findings
- One reference existed: the docs studies table (`studies_benchmarks.md`), now updated in this PR. No src/test/CI references to the file or to `run_cygnss_estimator_validation_cli`.
- No P1 findings.

## P1 Assessment
No P1 findings. Removal of an un-runnable script.
Unresolved P1: No

## Tests Added/Updated
- None; nothing to pin. The CI matrix on this PR is the no-dependency proof.

## Residual Risk
- When the perturbation-mc estimator-validation work lands, its driver should be created in the private telemetry repo rather than restored here.
