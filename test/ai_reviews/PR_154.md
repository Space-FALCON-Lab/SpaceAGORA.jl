# PR 154: native-free grid atmosphere and pinned wrapper retrieval

## Scope

Integrate the accepted grid atmosphere adapter, preserving the terrain work merged after the original `1ce7b22f` validation base. The reviewed runtime adapter, sampling/cache behavior and numerical assertions are unchanged. Test environment setup and the launcher were corrected after the first submitted CI run. Current-main mission-demo changes are reconciled separately from the atmosphere implementation.

Claude independently accepted source integration, software regression receipts and the exact native-free P20 replay at review commit `cf1a51c210928e4e779c645f6f20575922f5f606`, with no blockers. The accepted integration archive SHA256 is `60bea6fddae7a10173ac1ce9155ec4684719ffae705de2441197ef9a3211f927`. That review performed archive/patch/receipt and independent output comparisons; it did not run Julia. The delivery delta adds deterministic public/private wrapper pins, retrieval tests and CI wiring; it was checked separately from the accepted scientific experiment.

## Changed Files

- `src/SpaceAGORA.jl`: import, document and export the grid model alongside existing symbols.
- `src/environment/atmosphere/gram_grid_atmosphere_model.jl`: document and define the owned grid-backed density model.
- `src/environment/physical_models.jl`: include the model in EnvironmentModels.
- `src/simulation/callbacks/density_callbacks/config.jl`: classify the grid path for its intended direct sampling behavior.
- `src/simulation/callbacks/density_callbacks/runtime.jl`: preserve coordinate-dependent grid samples through callback routing.
- `src/simulation/engine/effector_sampling.jl`: bypass stale native buffer/vacuum approximations for grid queries.

Additional changes include the optional extension implementation, public API documentation, four grid regression files and their runner, the public wrapper gitlink, `.github/gramsuite-revisions`, the retrieval helper and its portable local-Git fixture test. Existing workflow step labels identify pinned revisions; the test matrix runs the retrieval fixture before dependency setup and the native-free adapter suite afterward. A dedicated grid-test project resolves SpaceAGORA and the selected local wrapper together. The workflow carries the identical 180-minute matrix allowance/comment from PR151; no tests are disabled or weakened.

## Findings

No blocking source integration issue was found. The adapter reuses the wrapper's interpolation owner rather than adding another kernel. The actual-engine domain escape remains an error, without native fallback or silent vacuum. A finite elapsed time does not evolve a frozen grid; either wind Boolean returns stored local ENU components. Exact-pole vector uniqueness and the absent pressure field remain documented limitations.

The public and private wrapper commits are `12eb2ee722881247970d38bbc2bb1ffa3a921523` and `26ff851a6d5337cf8d87162f5adc2bece70048dd`. Their nine shared Project/runtime/test files are identical to the accepted candidate. The public README matches it; the private README preserves private acquisition guidance. Both add the same native-free CI workflow. No native source/data or grids are changed by this delivery.

The retrieval helper verifies exact fetched and checked-out commits, rejects conflicting tracked edits, supports unrelated public/private histories and supplies temporary GitHub-scoped credentials without persisting tokens. A mode cannot substitute a floating branch. Existing docs/Copilot `dev || regular` fallback remains explicit caller policy, with both targets pinned.

The first CI run at `83f06fd3` failed before grid assertions: prepending the separately resolved vendored wrapper to `LOAD_PATH` shadowed the main dependency graph, and `StaticArraysChainRulesCoreExt` could not resolve `ChainRulesCore`. Run `35453867147`, job `105925676925`, records the failure. The dedicated test environment now develops both local packages in one operation, then starts the unchanged numerical suites in a fresh process. The launcher no longer mutates `LOAD_PATH`. Root project/manifest and wrapper source remain unchanged. The independently reviewed manifest has one coherent set of StaticArrays/ChainRulesCore dependencies and extension mappings.

Claude's three nonblocking wording corrections are preserved: SHA is new versus the old wrapper base but unchanged from the accepted wrapper; 16 matching `patch_check_v3` files were retained despite exclusion wording; escape traces differ in three closure-type lines beyond paths while the error and partial states are identical.

## P1 Assessment

Unresolved P1: No

Source integration is accepted within the stated contract. Current-main local compatibility and fixture retrieval checks pass. Full repository and documentation CI on the submitted head are still required before merge. This artifact is not approval of a public grid release or a scientific accuracy threshold.

## Tests Added/Updated

- CI environment correction: the exact dedicated setup and fresh-process runner pass 548/548 synthetic grid assertions locally on Julia 1.12.3 with four threads and native GRAM unavailable. The native wrapper remains uninitialized and no libgram is loaded. Test setup leaves the root project/manifest unchanged. The original failing CI log is retained; the new Linux CI run remains required. The subsequent main reconciliation includes only the three already accepted PR152 mission-demo files, without an atmosphere runtime change.
- Current-main native-free combined suite: 908/908 assertions, Julia 1.12.3, four threads, including 654 retained P20 samples and threaded real-grid checks, epoch/run isolation and worker construction recipes. Inputs remained unchanged, native wrapper stayed uninitialized and no libgram was loaded. This is the newly executed compatibility run on the delivery runtime.
- Portable pinned-retrieval regression: 15/15 local cases, including advanced default heads, unrelated histories, repeat calls, mode switches, invalid/unavailable/duplicate pins, conflicting edits and credential scope/persistence. It uses standard Python/Git and file-only fixture transport, with no network or native execution. The default CI invocation passes locally.
- Prior accepted integration: 646 wrapper assertions, 12 numerical-helper assertions and eleven byte-identical P20 regression tables containing 72,348 rows, plus exact events/preflight/solver metadata and domain-escape states. These remain the retained evidence on the original integration base; they were not relabeled as new full-repository runs.
- Both public and private wrapper CI workflows passed their native-free package tests on Julia 1.10 and 1.12. No native build or LFS download is part of those wrapper jobs.

The review-artifact gate and full SpaceAGORA CI are checked on the publication head. No new four-treatment scientific propagation or benchmark is introduced.

## Residual Risk

The wrapper references currently identify submitted branch commits. If downstream merging rewrites those identities, update the pins and gitlink together and rerun the affected CI. Ordinary SpaceAGORA CI still uses the private native-capable wrapper and existing native setup; this PR does not claim that the entire CI pipeline is public or native-free. Public/native CI restructuring remains separate.

The first real grid's distribution terms, named-preset configuration/retrieval layer, complete public scenario assets and clean unaided algorithm exercise remain open. Native GRAM remains optional for the intended user workflow. Stored bounds, mean frozen atmosphere, ENU/pole conventions and application-specific numerical requirements must govern advertised support. Passing software tests does not establish atmospheric or flight accuracy.
