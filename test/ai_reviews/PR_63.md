# AI Review — PR 63: emit the per-link wind warning at the actual discard site (PR #62 Codex follow-up)

## Scope
Codex P2 on the merged #62: the wind-limitation warning fired at the wrench gate, so a translational-only, root-only, or early-returning evaluation consumed the `maxlog=1` budget, and a later orientation-enabled multi-link simulation in the same process could then silently discard link-local wind without the promised disclosure.

## Changed Files
- `src/dynamics/coupled/aerodynamic_wrench_models.jl` — the gate-level `_warn_per_link_wind(p)` helper and its three call sites are removed; instead, the link-sampling branch of `_aero_pure_wrench` now captures the link wind sample and warns (once, `maxlog=1`) exactly when a **nonzero** wind vector is actually thrown away. This is strictly more precise than the previous condition (environment wind flag at the gate): the budget cannot be consumed by ineligible runs, and zero-wind models never warn at all.

## Findings
- P2 (addressed): warning relocated from intent (gate) to occurrence (discard). No behavioral change to any physics path — the warning is diagnostic only.
- No P1 findings.

## P1 Assessment
No P1 findings. Pure diagnostics relocation; all force/torque computations byte-identical.
Unresolved P1: No

## Tests Added/Updated
- None added: exercising the warning end-to-end needs an orientation-enabled multi-link wrench evaluation with a wind-bearing atmosphere model (heavier than the probe file's scope); the existing "Per-link atmosphere scoping" testset still pins the gating semantics, and the full probe file is green locally.

## Residual Risk
- The complete per-link wind treatment (per-link wind-relative velocity through Mach/q/drag/lift/crosswind) remains the tracked future work from PR #62.
