# PR 157: viewer documentation and Apollo public API

## Scope

Document merged viewer capabilities and their saved fields, explain native GRAM epoch alignment, and expose eight existing Apollo guidance/control bindings from the root module. Claude's two candidate commits are preserved separately and integrated with main after the atmosphere adapter and CDN packaging merges. No guidance law, control law, solver, atmosphere runtime, dependency or viewer implementation changes.

## Changed Files

- `src/SpaceAGORA.jl`: import, forward documentation for, and export the eight existing Apollo types/functions from the simulation model.
- `src/gnc/guidance/landing/apollo_descent_guidance.jl`: attach the misplaced guidance docstring to its model type and document selection and pairing requirements. Executable definitions are unchanged.
- `src/gnc/control/landing/apollo_descent_control.jl`: replace a cross-module documentation reference with a resolvable plain name. Executable definitions are unchanged.

Documentation changes cover the viewer guide, visualization/plume columns, GRAM epoch rules, stop conditions, the visualization ownership record, the API registry and a separate generated Lunar Landing API page.

## Findings

No blocking source finding remains. Root exports follow the repository policy that the root module is the stable user surface. Existing internal bindings remain available. The separate API page avoids adding all landing docstrings to the already large main API page.

Integration corrections include the newly merged CDN page packager in the path-tracing instructions, explicitly exclude atmospheric winds from the viewer heating approximation, avoid treating Float64 export as a metre-accuracy guarantee, and explain that custom save lists must request plume diagnostics. The existing native-free grid section and beginner atmosphere path are preserved.

## P1 Assessment

Unresolved P1: No

This is documentation and API exposure of existing functionality. It does not establish flight accuracy or close the independent terrain-backed landing demonstration.

## Tests Added/Updated

Executed by Codex on the integrated source before this artifact-only commit:

- All 23 repository contract gates pass, including the public API and documentation gates.
- Existing Apollo descent suite: 105/105 assertions pass.
- Independent root-import construction probe: 5/5 assertions pass, pairing guidance and control through the exported API and shared state.
- Documentation-stripped syntax comparisons: both landing implementation files match main's executable definitions.
- Strict documentation build passes with doctests and external link checking. All eight generated landing API anchors, saved-column anchors and stop-condition links are checked. Every registered public symbol has documentation.

Claude additionally validated his two original commits separately before integration; those results are not relabeled as runs on this head. No numerical implementation or test threshold was changed. The full unit tree, full simulations and Linux CI were not repeated locally; fresh PR CI remains required.

The first independent probe omitted required site coordinates; a separate syntax comparison initially retained a misplaced standalone docstring. Both harness issues were corrected and their failed attempts retained, without changing package source. The documentation build reports the existing main API page size warning (181.53 KiB against a 200 KiB error threshold).

## Residual Risk

The newly exported names become part of the supported API and may conflict with downstream unqualified names; subsequent compatibility changes should follow normal API policy. Rendering documentation describes existing approximations and optional browser support, not a new browser acceptance run. Native GRAM epoch reconstruction preserves recorded constructor settings, not evolved stochastic state or arbitrary handle mutations. Final merge requires green checks on the submitted head.
