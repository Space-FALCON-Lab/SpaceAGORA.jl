# PR #151: RPO/HYPR station demonstration and runtime repairs

## Scope

Integrate the configurable-station RPO example and ISS viewer demonstration from Evan's PR121 and Claude's retained candidate `32e5c8c1`. Preserve the Gateway dimensions, mass and 8 m² reference-area default. Repair the native solver lifetime and threaded planning defects exposed by that review. Bound the ISS reference speed and align its circular station with the orbital frame used by planning and clearance.

This is author validation. Claude's original review covered the five-file extraction; independent review of the new runtime and demonstration corrections is still required before merge.

## Changed Files

- `src/gnc/control/rpo_mpc/lqmpc.jl`: share the solver-construction owner between initialization and controller copying; each copied controller owns a newly finalized native workspace, retains its primal warm-start vector and rebuilds native caches.
- `src/gnc/guidance/rpo/hypr/pso_path_planning.jl`: use particle-owned random streams and isolate candidate-path and cost-component temporaries during threaded evaluation. Ordered global-best updates and threading remain.
- `examples/Earth_RPO_CubeSat_MPC.jl`: configurable station inputs, optional reference-speed cap, and orbital-rate station attitude for the circular equatorial example.
- `scripts/dev/viewer_demos/iss_hypr.jl`: reproducible model sampling, recorded flown-plan reuse, output provenance, ISS reference speed 0.1 m/s and viewer overlays.
- Two registered RPO test files and user documentation complete the package.

## Findings

Three failed or interrupted attempts are retained. An RNG-only repair still produced different repeated plans at four threads; shared temporary bindings in cost evaluation were also racing. Isolating them restores exact one/four-thread snapshots. The first uncapped full ISS run had 20 sampled keep-out crossings and 2.95 m arrival error despite a clear geometric path. Its reference started at 0.569 m/s from a stationary spacecraft and contained a 1.185 m/s² discrete speed change, above its 0.009615 m/s² initial per-axis thrust authority. The final demonstration uses the identical geometric path and cost with a 0.1 m/s reference cap; it passes the same acceptance thresholds. A separate station-frame check found a fixed inertial attitude inconsistent with the RTN geometry; the circular station now rotates at its orbital rate.

The speed-only validation was stopped before its full run so the final attitude correction could be validated together. No source failure is inferred from that interrupted attempt.

## P1 Assessment

The copied controller remains usable after explicitly finalizing the original solver, with a command compared against an independent analytical QP solution. Repeated seeded plans and cross-process one/four-thread results agree exactly in executed cases. Final full-run sampled clearance is positive and every saved station attitude matches an independent orbital basis. There is no known blocking source finding after these author checks; independent review and current-head CI remain required.

## Tests Added/Updated

Initial repair: 129 assertions at one thread and four threads, including exact plan/cost/history snapshots for three seeds. Final behavior candidate `4c123a24`: 137 focused assertions, 24 fresh/reused smoke/full demonstration assertions, 60 neighboring RPO assertions and two unchanged broken markers. The geometric path and planner cost are identical before and after the demo speed cap.

The full 2880.6-second simulated approach has 1,442 saved rows, no sampled keep-out crossings, minimum clearance 2.7472 m, maximum tracking error 0.4126 m and final target error 0.0624 m. The station rotation differs from the orbital basis by at most 7.35e-14 in the executed matrix comparison. The smoke run also passes. Replay preserves the CSV and recorded plan hashes.

Browser acceptance of the actual exported full run passes station selection, Follow, playback, seeking, camera orbit, inertial/planet-fixed display and planned-path visibility. Both paths and the ISS model load; captured warning/error logs are empty. Video export was not tested.

All 23 contract gates and strict documentation with external links pass. The documentation-only follow-up `a6c679d7` leaves all executed runtime, example and test files unchanged. The final numbered artifact gate is checked on the committed publication head; fresh Linux CI remains pending.

## Residual Risk

The geometric retimer does not guarantee a full acceleration profile from rest, and LQ-MPC does not impose collision constraints. The bounded demonstration is validated against its sampled station point cloud, not operational ISS geometry or a continuous collision certificate. Arbitrary tumbling targets, different orbits/configurations, video export, full Linux CI and performance gains are not established. Native GRAM is not used; the executed simulations use SPICE and no atmosphere.

Seeded plans change from older versions. Fixed iteration budgets support reproducibility across thread counts; wall-clock stopping criteria do not. Controller copying preserves stored primal state but rebuilds native factorization, dual and diagnostic caches, so it is not an exact optimizer checkpoint. The final documentation-only follow-up is separate from the executed behavior candidate.

Unresolved P1: No
